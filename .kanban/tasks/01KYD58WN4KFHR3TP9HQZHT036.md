---
comments:
- actor: claude-code
  id: 01kyfgk4wseqepmwft0psc4ar4
  text: |-
    Corrected against the vendored `Schema/acp-v2.json` (`schema-v2.0.0-alpha.2`, vendored in M0) and the current tree.

    **The 1-based line-number invariant does not exist in v2.** The card asserted "**1-based line numbers.** Same treatment [as absolute paths]", with an acceptance criterion and a test demanding that 0-based line numbers fail decoding. Implementing that would **reject payloads v2 permits**. The schema has exactly one line-valued field, `ToolCallLocation.line`, typed `["integer","null"]` with `minimum: 0` and described only as *"Optional line number within the file."* No base is stated there or on the v2 tool-calls page.

    This is a **deliberate divergence from v1**, which did state 1-based — flagged as such in the description so it reads as a decision, not a lapse. The rule is inverted rather than deleted: the test now requires `line: 0` (and omitted, and `null`) to decode, and explicitly says the v1 "0 must fail" test must not be recreated.

    `wireInvariantFields` recorded as **empty by design**: v2 gives absolute paths a first-class `AbsolutePath` `$def`, so the invariant rides each field's `$ref` and a per-field override table would just be a drift-prone second copy of the schema. `GeneratorConfig.acpV2` documents this in place. The override mechanism stays for a future revision that states an invariant in prose alone.

    Other stale claims fixed, verified against the tree:
    - `Core/ProtocolVersion.swift` and `Core/ForgivingDecoding.swift` are **already restored** (M0 brought them back so the regenerated output would compile; `ProtocolVersion` now carries `.v2`). The card listed them as deleted and told the implementer to restore them "before anything else". The generated output compiles today; that opening step is gone.
    - `Core/LineNumber.swift` moved out of "rebuild" and into an explicit do-not-restore: the generator emits no reference to it, because there is no `.lineNumber` mapping and no v2 field to attach one to.
    - The rebuild list is now just `Core/ACP.swift` and `Connection/RequestError.swift`.
    - `ForgivingDecodingTests` stays in scope, but the parenthetical "deleted with `ForgivingDecoding.swift`" was corrected -- the file is back, the tests are not.

    Also added: a short *placeholder seam* section pointing at `plan.md` -> M1 for the four deferred union shapes. M2 (^fzv61ky), M5 (^ht1sh5g), and M7 (^4vnprpg) now each carry a "blocked on the M1 seam" note naming the surface it untypes for them, and those references would otherwise dangle. The full instructions are left in `plan.md` rather than duplicated here.
  timestamp: 2026-07-26T15:27:12.793759+00:00
- actor: claude-code
  id: 01kyfvpmv8mjbkxddskg7ty4a5
  text: |-
    ## Picked up after M0 closed — what carries over

    *(Restored. This comment's own warning about markdown tables fired on itself: the serializer wrapped the line carrying the literal separator row so that the three dashes landed at column zero, which split the YAML document, truncated this comment at that point, and left the card reading `title: Untitled` with its front matter stranded in the description body. Rewritten below with the separator described in prose instead of quoted. The text is otherwise the original.)*

    M0 is `done` (commits `ed220ab` → `e094323`, six on `v2-reset`, none pushed). `schema-v2.0.0-alpha.2` is vendored, the generator is re-pointed and taught v2's `anyOf` vocabulary, and the suite is at 64 tests / 9 suites green with the CI codegen diff gate restored and verified able to catch a hand-edit.

    **This card was already corrected against the vendored schema**, so read it as current. Two of its original instructions are now wrong and have been fixed: `ProtocolVersion` and `ForgivingDecoding` were listed as deleted with an instruction to restore them "before anything else" — M0 already restored both (`ProtocolVersion` carries `.v2`), and the generated output compiles today. And the "1-based line numbers, enforced at decode time" requirement is gone: the vendored schema has exactly one line-valued field, `ToolCallLocation.line`, described only as "Optional line number within the file" **with `minimum: 0`**. It explicitly permits zero. Enforcing 1-based would reject payloads v2 allows. That is a deliberate divergence from v1, which did state 1-based.

    ### The real work: close the placeholder seam

    Four definitions defer to `JSONValue` today, in two shapes that fail in opposite directions — `plan.md` → *Starting point* has the full table with the API surface each one untypes. **Take `SetSessionConfigOptionRequest` first.** It is the cheapest and the most expensive to leave: the only deferral that untypes a *routed stable method's entire params object*, so M2 would otherwise generate `setSessionConfigOption(_ params: JSONValue)`. `plan.md` → M1 carries the three-step recipe, and step 3 matters — skipping it makes generation *throw* rather than emit.

    Then the three tagged unions (`AuthMethod`, `PlanUpdateContent`, `ReplayFrom`), which need `unknown(String, JSONValue)` so the fallback can carry the payload it currently drops.

    **Write the round-trip test in the same change, not after.** An unexercised two-associated-value `Codable` case is exactly the "preservation asserted rather than real" failure this milestone exists to close. The harness already exists — `ACPGenerateTests` depends on `ACPGenerateCore`, which depends on `FoundationModelsACP`, so the generated module is already linked into the test binary and the test costs one `import`. An earlier agent claimed this was untestable, checked, and withdrew the claim.

    **Completion signal:** resolving rows 1–4 makes **eight** structs reachable that nothing references today. Six JSON-RPC envelope types will never be reached by any row and must be excluded from the signal. Verify the enumeration yourself — this count has been wrong twice.

    ### Traps that have already cost time

    - **Never run `swift format`.** No config exists; it reformats the whole 4-space codebase to 2-space including the checked-in generated output the CI gate pins byte-for-byte. Four agents have hit this and had to revert wholesale.
    - **Never put a markdown table in a kanban comment.** A table's separator row — the one made of dashes between pipes — parses as a YAML document delimiter and corrupts the task record. That is not hypothetical: it is what happened to this comment. Bullet lists only. Tables in `plan.md` are fine.
    - **Watch for stale git worktrees under `.claude/worktrees/`.** One was silently intercepting `files` reads and edits while `grep files` on the same absolute path returned `HEAD` — an agent editing through it would have written to a detached tree and watched the change vanish. Cleared, but check `git worktree list` if edits behave strangely.
    - **The hash stamp hides generator changes.** `rm Sources/FoundationModelsACP/Generated/.schema-hash` to force a run.

    ### On claims

    Four confident exhaustiveness claims failed during M0 — the orphan triage, "the only union stage with this defect", "zero `$ref`s anywhere", and "seven read sites". Every one passed its own internal check while being wrong. If you state a count or an "only", derive it and say how.
- actor: claude-code
  id: 01kyfwj51xe2ezkp0e1zzgbhtg
  text: |-
    Seam rows 3 and 4 closed; rows 1 and 2 next.

    **Row 4 — `SetSessionConfigOptionRequest`** (taken first, as instructed). The three coupled `SchemaGenerator.swift` edits landed, plus one the recipe did not name: `unionVariants(of:)` is the shared catch-all filter, and the value-union stage now needs the *opposite* answer from every other stage. Split into `unionVariants(of:)` (drops every catch-all — the string-enum, tagged-union, and discriminated-union emitters synthesize their own fallback) and `valueUnionVariants(of:)` (keeps a catch-all that declares a member beyond the discriminator, because there the catch-all *is* the default case). Both read `declaredUnionVariants(of:)`. Skip that split and the bare catch-all in `objectValueUnionWithBareFallbackSchema` becomes a second default and generation throws `expected exactly one default value variant, found 2`.

    `ValueUnionCaseModel.tag: String?` became `selector: ValueUnionSelector` with three cases — `.tag`, `.untagged`, `.capturedTag`. Two boolean-ish fields would have been an implicit invariant; the union genuinely has three states, because the pre-existing discriminator-less default (`objectValueUnionSchema`'s `text`) still exists beside the new catch-all and still takes one associated value and re-encodes no tag.

    **Row 3 — `AuthMethod`, `PlanUpdateContent`, `ReplayFrom`.** `isUnknownFallbackVariant` lost its `pinned:` parameter entirely: with a fallback that carries payload, what a catch-all declares no longer decides anything. The `const`-pinning half of the old predicate is what still defers (`valueUnionWithConstPinningFallbackSchema`), so that guard stayed in `classifyAnyOf`.

    **`unknown(String)` -> `unknown(String, JSONValue)` is an emitter-wide change, not a per-definition one.** `taggedUnionDeclaration` and `discriminatedUnionDeclaration` each emit one fallback shape for every union they render, so all 19 unions in `Unions.generated.swift` gained the payload-carrying fallback, not just the three. That is the right answer anyway — a `session/update` variant a newer peer adds was being truncated too — but it is worth knowing the diff is wider than the card's three names.

    **The payload is the raw object *less* the discriminator**, via two new `JSONValue` members in `Core/JSONValue.swift`: `init(from:excludingMember:)` and `encodeMembers(to:)`. Capturing the whole object instead would put the tag in two places that can disagree. `encodeMembers` writes through a dynamic-key keyed container — `JSONValue.encode(to:)` uses a *single-value* container, and calling that on an encoder that already vended a keyed container traps rather than merging, so `try payload.encode(to: encoder)` (what the struct-payload cases do) is not available here.

    **Verification, not assertion.** `Tests/FoundationModelsACPTests/` is new (added to `Package.swift`) and holds `UnknownFallbackRoundTripTests`, 10 tests. Round-trips compare parsed `JSONValue` trees, not text, since member order is not part of a JSON document's value. Checked the cover is real: reverting the emitter so the fallback drops tag and payload turns **7 of the 10 red**; restored and green again.

    Suite: 84 tests / 10 suites. `swift build` and the generated-output diff gate are green at this point.
  timestamp: 2026-07-26T18:56:23.101116+00:00
- actor: claude-code
  id: 01kyg254hm56d1bmndncr8mkag
  text: |-
    ## M1 complete — seam closed, green, ready for review

    **Final state.** `swift build --build-tests` clean. `swift test`: **123 tests / 16 suites**, zero failures (was 64 / 9). DocC builds clean under `--warnings-as-errors`. Regenerating from the vendored artifacts with the hash stamp dropped is a byte-for-byte no-op, so the CI codegen diff gate passes.

    ### Scope: the seam was six definitions, not four

    The card's *placeholder seam* section and the handover both say "four". `plan.md`'s table has **five rows of deferrals**, and rows 1-4 hold **six** definitions — the four named plus `ErrorCode` (row 1) and `DiffChange` / `SessionConfigOption` (row 2). Since the completion signal is defined over rows 1-4, resolving only the named four could never have made it go green. All six are resolved. Row 5's eight stay deferred, deliberately.

    Two of the six needed a new emission family rather than a fallback change:
    - **`ErrorCode`** — the enum stage was string-only. It is now scalar-kind-driven by one `enumRawKinds` table mapping the JSON `type` keyword to the raw Swift type, so `ErrorCode` emits `unknown(Int)`. An integer cannot name its own case, so integer-enum cases are named from each variant's `title` through a new `swiftCaseName(fromTitle:)` that splits on whitespace as well as underscores.
    - **`DiffChange` / `SessionConfigOption`** — new `.objectTaggedUnion` family: base struct plus a nested `Payload` enum. Nested name is fixed rather than derived from the discriminator, because `SessionConfigOption`'s discriminator is `type` and `SessionConfigOption.Type` is metatype syntax. `objectValueUnionDeclaration` and `objectTaggedUnionDeclaration` share one `objectCarryingAUnion` renderer; the struct scaffolding was identical.

    ### Completion signal, derived rather than trusted

    Ran the same orphan analysis over `HEAD`'s generated output and over the working tree: **26 orphan declarations before, 16 after**, and the set difference is exactly the ten `plan.md` names (`AuthMethodAgent`, `DiffPathChange`, `DiffPathPairChange`, `PlanItems`, `ReplayFromStart`, `SessionConfigBoolean`, `SessionConfigId`, `SessionConfigSelect`, `DiffFileType`, `SessionConfigOptionCategory`). Total declarations unchanged at 146 both times — the placeholders became structs and enums in place. Method: parse every top-level declaration, then ask whether any *other* declaration mentions its name as an identifier or as a routing-table string literal.

    Two corrections to the handover's phrasing of that signal. It says "eight structs"; the group is **ten declarations** — eight structs and two enums, which is what `plan.md` says. It says "six JSON-RPC envelope types"; there are **five** (`AgentRequest`, `AgentNotification`, `ClientRequest`, `ClientNotification`, `ProtocolLevelNotification`), and the sixth is presumably one of the two routing roots, which `plan.md` counts separately.

    ### What the adversarial review caught, and what it cost

    Ran `double-check`. It returned REVISE with seven findings. Two were real defects in shipped behaviour, and both are now fixed with cover:

    - **A hand-built fallback payload could hijack the discriminator.** Two keyed containers over one encoder share an object and the *later* write wins, so `AuthMethod.unknown("_vendor_sso", ["type": "agent"])` encoded `{"type":"agent"}` — silently re-decoding as a different variant — and a nested payload could overwrite a base property. Wire-sourced values were safe because decode drops those names, but the cases are public and take two associated values. `encodeMembers(to:reserving:)` now rejects a payload claiming an owned name, and the emitter emits the same list on both arms from one helper so the two cannot drift.
    - **My exhaustive tag probe was swallowing 37 of 44 tags.** It treated any `DecodingError` as "the tag matched", but a union reading the *wrong* discriminator key throws `keyNotFound` before comparing any tag, so nine of thirteen unions were asserting nothing. My first fix for this was also wrong — it compared `key.stringValue` to the discriminator, which a wrong-key union never produces — and I only found that out because the mutation test I ran to prove the fix did not go red. The working answer is a separate premise test, `everyUnionReadsTheDiscriminatorItsVariantsPin`, which decodes an unmatched tag per union outside any catch.

    The other five were a wrong count in a doc comment (my `_meta` classifier missed a description that wraps "are\nequivalent" across a newline — it is five, not four), a stale "8 of them" in a generator comment that this change itself invalidated, a missing collision guard on `objectValueUnionModel` that its sibling stage had, an unsanitized `valueEnumName`, and an `ACP.swift` doc describing a directory that does not exist. All fixed; the two new generator guards are covered by tests that pin the detail string rather than just asserting "it threw".

    **Re-verification was requested and did not return** after ~35 minutes. The gate is advisory in `really-done` and bounded to one re-check; the hard requirement — verification commands green — is satisfied above, and every finding from the completed round was acted on rather than argued past. One suggestion was deliberately not taken: guarding a *flattened payload's* member names against the base object's. No vendored payload collides today, and it belongs with whatever change introduces one.

    ### Verified by mutation, not by assertion

    Each behavioural claim was checked by breaking the production code and watching the right tests go red:
    - Fallback drops tag and payload -> **7 of 10** `UnknownFallbackRoundTripTests` red.
    - Every emitted tag mangled -> **exactly 44** probe failures, matching the 44 tags derived independently from the schema.
    - Integer enums removed from `enumRawKinds` -> both `ErrorCode` tests red.
    - Owned-member guard stubbed out -> the hijack test red.
    - Emitted `CodingKeys` raw value mangled -> `everyUnionReadsTheDiscriminatorItsVariantsPin` red.
    All restored and green after each.

    ### Left deferred, and why

    - **Three-state patch semantics.** v2 gives upsert fields three wire states — omitted leaves unchanged, `null` clears, a value replaces — and a Swift `Optional` has two, so a client meaning "clear" currently sends "unchanged". Six `_meta` fields say so explicitly and the same rule governs their siblings' other fields. Carded as ^1pfngj1, noted on M7 (^4vnprpg), and pinned in its current-behaviour form by `MetaFieldTests.upsertMetaCannotYetDistinguishOmittedFromNull` so it fails loudly when fixed. The one acceptance-criteria checkbox left unticked.
    - **Row 5's eight placeholders.** Three are the extension escape hatch with no stated shape; five are untagged unions whose branches pin no discriminator. `VendoredSchemaTests.onlyTheDeliberatelyFreeFormDefinitionsStayUntyped` pins the list by name.

    ### For the next agent

    - **This card's own record was corrupted** by the handover comment: a wrapped line put a table separator at column zero, which split the YAML document, truncated that comment mid-sentence, and left the card reading `title: Untitled` with its front matter stranded in the description body. Repaired through the kanban API — comment restored with the separator described in prose, title and description and `depends_on` restored. The warning in that comment was right; it just could not survive stating itself literally.
    - **`unknown(String)` -> `unknown(String, JSONValue)` is emitter-wide**, so all 11 stand-alone tagged unions changed, not just the three named. That is why the generated diff is large.
    - The `.discriminatedUnion` family matches **zero** vendored definitions — only synthetic generator tests reach it. Worth knowing before assuming a change there is covered by the vendored suite.
    - `swift format` was never run, per the standing warning.
  timestamp: 2026-07-26T20:34:08.052103+00:00
- actor: claude-code
  id: 01kyg3spdcjkf9g1fe4ff9pq9k
  text: |
    Review pass on c9810f3 (scope HEAD~1..HEAD). Engine: 48 checks attempted, 6 confirmed, 8 refuted. Five findings recorded; one dropped.

    Dropped: a finding asking to delete the `assertRoundTrips` and `canonicalized` helpers in `Tests/ACPGenerateTests/TaggedUnionTests.swift`. That file is pre-existing and this commit adds exactly one line to it, so the finding targets test code that already existed. The review skill's blanket exception forbids recording findings that refactor pre-existing tests.

    Scoping notes on what remains, for whoever picks this up:

    * The four focus areas came back clean. No finding landed on `encodeMembers(to:reserving:)`, on unknown round-tripping for the tagged unions or the value union, on the new objectTaggedUnion decode/encode symmetry, or on the unionVariants and valueUnionVariants split.
    * The two generated-output duplication findings on `Models.generated.swift` are cosmetic. Each cited pair is the decode side `excludingMembers` and the encode side `reserving`, and both are emitted from the single `ownedMembers(discriminator:siblingMembers:)` helper in `Emitter.swift` (call sites at lines 346 and 365). One source of truth, so the drift the finding warns about cannot occur. A fix means an emitter change plus regeneration of a byte pinned artifact.
    * The value union finding concerns the `.untagged` default path. The only value union in the generated output takes the `.capturedTag` path, so no shipped type is lossy today. It is latent generator capability plus a docstring accuracy question.
    * The Emitter duplication finding spans `unionStructInit` (new in this commit) and `memberwiseInit` (untouched, outside every diff hunk). Extract a helper on untouched pre existing code was ruled out of scope by precedent in M0 rounds 3 and 5.
  timestamp: 2026-07-26T21:02:50.284463+00:00
- actor: claude-code
  id: 01kyg3wdj53c331vxw88w9ska6
  text: |
    ## Stopping here — M1 left in `review` with five open polish findings, by choice

    The work is done and green: `c9810f3`, 123 tests / 16 suites, DocC clean, codegen regeneration a byte-for-byte no-op. The closing review found **no substantive defects**, and all four correctness areas it was pointed at came back with zero engine findings — the `encodeMembers(to:reserving:)` boundary, `unknown(String, JSONValue)` and `other(String, JSONValue)` round-tripping, `.objectTaggedUnion` decode/encode symmetry, and the `unionVariants` / `valueUnionVariants` split.

    **I am not moving this to `done`, and not working the five findings either.** Both would be wrong:

    - Forcing `done` with open findings is out of bounds; the gate exists precisely so a task advances on a clean review, not on an orchestrator's judgment.
    - But working them would regenerate a byte-pinned artifact for zero correctness gain. Three target repeated literals in `Models.generated.swift` whose premise is false — both sides come from one emitter helper, so the drift they guard against cannot occur. One is latent and unreachable (the only value union in the generated output takes the other path), reducing to docstring accuracy. One is the extract-a-helper-on-untouched-code pattern already settled as out of scope twice during M0.

    So the honest state is: **green, reviewed, and awaiting a human decision** on five style items against generated output. Recorded in the follow-ups card `^emz2swz` rather than churned here.

    ## What a human should weigh in on before M7

    The one genuinely open design question from M1 is carded as `^1pfngj1`: **three-state patch semantics**. v2's upsert `content` has three states — omitted means unchanged, `null` or `[]` clears, a value replaces — and a Swift `Optional` has two. A client meaning "clear this message's content" currently sends something indistinguishable from "leave it alone." That is silent history corruption on the wire, it affects six upsert types, and it belongs to M7. It is pinned by `MetaFieldTests.upsertMetaCannotYetDistinguishOmittedFromNull` so the limitation is visible rather than latent, but the fix is a design choice, not an implementation gap.

    ## Correction to this card's own handover comment

    The briefing I left at pickup said the seam was four definitions and that eight structs would become reachable. Both wrong. Derived from the diff afterwards: **seven** definitions resolved across four shape rows (`ErrorCode`; `DiffChange`, `SessionConfigOption`; `AuthMethod`, `PlanUpdateContent`, `ReplayFrom`; `SetSessionConfigOptionRequest`), and **ten** names became reachable, eight structs plus two enums. The eight survivors reconcile exactly to row 5 of `plan.md`'s table. Three parties produced three different counts for this before anyone ran the one-line check that settles it.

    That handover comment also corrupted this card's YAML on arrival, leaving it reading `title: Untitled` — the warning it contained about the table separator reproduced the separator itself, inside backticks, which gives no protection. Repaired before implementation began.
  timestamp: 2026-07-26T21:04:19.525125+00:00
- actor: claude-code
  id: 01kyhz7qqbh51tqj5ymjrjsyr3
  text: |-
    Implemented all 5 open review findings from 2026-07-26. All 5 checkboxes in the description are now checked.

    Finding 1 (docstring vs. lossy untagged default): chose option 2 (fix the docstring), not option 1 (reject the shape) or option 3 (force capturedTag). Verified against the actual test fixtures before picking: objectValueUnionSchema and objectValueUnionFiltersTheBareUnknownFallback in AnyOfUnionTests.swift already exercise a tagged case coexisting with a discriminator-less untagged default as the primary documented value-union shape, so option 1 would have broken the feature's own reference tests. Option 3 would force strict container.decode for a variant whose schema declares no discriminator property at all, turning today's valid "absent discriminator" decode into a hard failure -- a regression, not a fix. So the code's behavior (fall back to the default on absent-or-unrecognized, without preserving an unrecognized tag when the variant's own schema carries no slot for one) is genuinely correct for that schema shape; only the docstring overclaimed. Rewrote valueUnionDecoder's doc comment to say plainly, for the untagged branch, that the value doesn't round-trip when unrecognized-but-present; tightened the capturedTag branch's doc to say it does.

    Findings 3+4 (and the same root cause behind the general shape mentioned for finding 4's DiffChange/SessionConfigOption pair): added Emitter.excludedMembersDeclaration(discriminator:siblingMembers:), emitted once per union body (right after CodingKeys), and pointed unknownDecodeArm/unknownEncodeArm at it instead of each independently re-rendering ownedMembers(...) into an inline literal. This fires uniformly through taggedUnionDeclaration and discriminatedUnionDeclaration, so every stand-alone union in Unions.generated.swift and every object-nested Payload enum in Models.generated.swift picked up the constant in one regeneration, not just the two cited sites.

    Trap hit and worth recording: a bare (unqualified) `excludedMembers` reference from inside an instance method (init(from:)/encode(to:)) does not compile in Swift -- confirmed with a minimal swiftc -typecheck repro, both for a top-level enum and for one nested inside a struct. Needs `Self.excludedMembers`. First regeneration attempt used the bare form and broke swift build across every generated union; caught it via the diagnostics/build loop before it went further, reverted the generated files with `git checkout -- Sources/FoundationModelsACP/Generated/` to get back to a compilable baseline (needed because ACPGenerateCore depends on FoundationModelsACP, so `swift run acp-generate` can't run once the checked-in generated code stops compiling), fixed the emitter to emit `Self.excludedMembers`, and regenerated clean.

    Finding 2: added renderParameters(items:render:) exactly as suggested and used it from both memberwiseInit and unionStructInit. Pure emitter-internal refactor, no generated-output change.

    Updated one test assertion that was pinned to the old inlined-literal generated text: AnyOfUnionTests.objectCarryingATaggedPayloadUnionEmitsANestedPayload now asserts the private static let excludedMembers = [...] declaration and the Self.excludedMembers-qualified decode call instead of the old literal.

    Verification: swift build clean, swift build --build-tests clean, swift test = 123 tests / 16 suites, 0 failures (52 in FoundationModelsACPTests + 71 in ACPGenerateTests, matching the pre-change baseline count). checkedInOutputMatchesAFreshRun stays green -- regenerating from the vendored schema after these emitter changes is byte-identical to the committed output except for the intended diffs (the new excludedMembers constants + Self.-qualified references, and the corrected value-union docstring on SetSessionConfigOptionRequest.Value). DocC (`swift package generate-documentation --target FoundationModelsACP --warnings-as-errors`) builds clean. No swift format run. Diagnostics on the working diff: 0 errors, 0 warnings (one pre-existing, unrelated Sendable-metatype warning in TaggedUnionRoundTripTests.swift, a file untouched by this change, is outside the diff scope).

    Leaving in `doing` per the /implement skill -- ready for /review.
  timestamp: 2026-07-27T14:21:36.363167+00:00
- actor: claude-code
  id: 01kyhzb5egtpj511x4v3aegjc6
  text: 'Self-caught issue: my prior description update accidentally double-escaped newlines (pasted the JSON-escaped "\n" from a get-task response as literal backslash-n text instead of real line breaks), which collapsed the whole description onto one line and dropped the progress fraction to 0.0. Fixed immediately with a follow-up update using real newlines; progress is back to 0.9473684210526315 (18/19 checkboxes, matching the one intentionally-open `_meta` three-state item). No corruption of the YAML/comment structure this time -- this was a plain string-escaping mistake in the description field, not the three-dash table issue from other cards.'
  timestamp: 2026-07-27T14:23:28.720778+00:00
- actor: claude-code
  id: 01kyhzx93c8hr8n2emrbbkps3p
  text: |-
    Adversarial double-check (independent agent, fresh evidence, not taken on faith): PASS, zero findings.

    It independently: confirmed the working tree touches only the claimed files (no swift format reformat); deleted .schema-hash and re-ran swift run acp-generate, then diffed the fresh output against the pre-run files byte-for-byte -- Models.generated.swift, Unions.generated.swift, and .schema-hash all came back IDENTICAL; grepped every excludingMembers/reserving call site across both generated files (AuthMethod, AvailableCommandInput, ContentBlock, McpServer, PlanUpdateContent, ReplayFrom, RequestPermissionOutcome, RequestPermissionSubject, SessionUpdate, StateUpdate, ToolCallContent, plus the nested DiffChange.Payload / SessionConfigOption.Payload) and confirmed every one uses the qualified Self.excludedMembers form, none bare; traced the actual capturesTag branch logic in valueUnionDecoder against the new docstring wording and confirmed they match; confirmed via objectValueUnionSchema that the tagged-case-plus-untagged-default shape is indeed the shipped fixture backing the rejection of the other two finding options; confirmed the renderParameters refactor is semantically identical to the old inline loops; confirmed the updated AnyOfUnionTests assertion is a strict superset of the old coverage (checks the new constant declaration plus the qualified reference, not a narrower check); grepped Tests/ independently for any other excludingMembers:[ / reserving:[ literal-pinning and found only JSONValueTests.swift, which exercises the JSONValue API directly and doesn't pin generated source text; and ran a full clean rebuild (rm -rf .build && swift build --build-tests) plus swift test, landing on the same 123/0 result.

    Per really-done's contract: verification-command pass is the hard requirement and is green; the adversarial sign-off is advisory and came back PASS with no findings to act on or justify past. Task is done and green.
  timestamp: 2026-07-27T14:33:22.284781+00:00
- actor: claude-code
  id: 01kyj41rbmtzs2paw04cdyer2t
  text: |-
    Implemented all 7 findings from the 2026-07-27 review round.

    Findings 1-3 (doc-comment first-line-must-be-a-period-terminated-sentence in Emitter.swift): merged each wrapped two-line /// first sentence into one line, exactly as specified, for renderParameters, ownedMembers, and excludedMembersDeclaration. No other doc content touched.

    Findings 4-6 (generated-name acronym casing): did NOT hand-edit the generated file. Instead added GeneratorConfig.knownAcronyms: Set<String> (set to ["MCP", "HTTP"] for .acpV2) and SchemaGenerator.applyKnownAcronymCasing(to:), wired into emittedName(name:). It splits a PascalCase name at capital-letter boundaries and uppercases any word matching a known acronym case-insensitively, so the fix is uniform rather than per-name. Derived the full affected set by grepping $defs keys in Schema/acp-v2.json for Mcp/Http (case-sensitive on definition names) before regenerating: 7 definitions, not 3 -- McpServer, McpCapabilities, McpHttpCapabilities, McpServerHttp, McpServerStdio, McpStdioCapabilities, HttpHeader. All 7 became MCPServer, MCPCapabilities, MCPHTTPCapabilities, MCPServerHTTP, MCPServerStdio, MCPStdioCapabilities, HTTPHeader after `rm Sources/FoundationModelsACP/Generated/.schema-hash && swift run acp-generate`.

    Note the two originally-cited findings (4 and 5) literally asked for "McpServerHTTP" and "McpHTTPCapabilities" (Mcp left mixed-case) -- the uniform rule instead produces "MCPServerHTTP" and "MCPHTTPCapabilities" (Mcp also fixed). Verified this is correct, not just different: derived the full before/after identifier set from the schema rather than trusting the findings' literal suggested strings, since finding 6 already contradicted findings 4/5 by fixing Mcp on its own name -- applying the acronym rule non-uniformly across the three would have been the actual bug.

    Updated hand-written references to the renamed real type: Tests/ACPGenerateTests/VendoredSchemaTests.swift (two string-literal pins -- the declared-type-names list in everyTaggedUnionCarriesAPayloadBearingFallback, and the AbsolutePath-property pin in absolutePathDefinitionResolvesToTheHandWrittenInvariant) and Tests/FoundationModelsACPTests/TaggedUnionRoundTripTests.swift (two McpServer.self -> MCPServer.self references). Left Tests/ACPGenerateTests/GeneratorCoreTests.swift's inline "McpServer" schema fixture untouched -- it generates through its own miniatureConfig, which does not set knownAcronyms, so it's a deliberate no-op there, verified by the suite staying green.

    Checked declarationsAreEmittedInSortedSchemaNameOrder (which reconstructs original-schema spelling only via config.typeRenames, not the new acronym casing) still passes: worked out by hand that every acronym-renamed identifier's first point of difference from its sorted neighbors is unaffected by the casing of "Mcp"/"Http" within the word, so the test's sort check holds for the right structural reason today, not by luck.

    Finding 7: added the missing encode-arm assertion in AnyOfUnionTests.objectCarryingATaggedPayloadUnionEmitsANestedPayload, next to the existing decode-arm one, without touching other assertions.

    Verification: swift build --build-tests clean; swift test = 123 tests / 16 suites / 0 failures (52 FoundationModelsACPTests + 71 ACPGenerateTests, matching pre-change baseline exactly) -- reproduced via a full `rm -rf .build` + rebuild, not just incremental. DocC (`swift package generate-documentation --target FoundationModelsACP --warnings-as-errors`) clean. checkedInOutputMatchesAFreshRun (the codegen diff gate) green -- regeneration after the emitter/config change is byte-for-byte what's committed. No swift format run.

    Side note: the `rm -rf .build` clean-rebuild step hit an unrelated environment issue -- .build/checkouts/swift-docc-plugin had a deeply self-nested checkout structure that resisted deletion (stale from something unrelated to this change) and needed a few repeated `rm -rf` passes before SwiftPM could re-clone it. Not a code issue, just noting it in case it recurs.

    Adversarial double-check dispatched; will record its verdict once it returns. Leaving in `doing` per /implement.
  timestamp: 2026-07-27T15:45:43.284215+00:00
- actor: claude-code
  id: 01kyj4hak1e2vwnpsjxwyhtwy9
  text: |-
    Adversarial double-check (independent agent, fresh evidence): PASS.

    It independently: confirmed the three Emitter.swift doc-comment merges match the findings' exact wording with nothing else disturbed; hand-traced applyKnownAcronymCasing's no-op cases (no matching word, already-cased names like ACPError/RequestID, and config.knownAcronyms empty as in GeneratorCoreTests.miniatureConfig); grepped Sources/ and Tests/ and confirmed every old Swift type reference (McpServer, McpServerHttp, McpHttpCapabilities, McpServerStdio, McpCapabilities, McpStdioCapabilities, HttpHeader) is gone except deliberate illustrative doc comments, a schema-description string copied verbatim, the unaffected GeneratorCoreTests fixture, and the definition: "McpServer" label string in TaggedUnionRoundTripTests (correctly left alone, its adjacent .self references updated); ran a fresh full-clean-rebuild swift build --build-tests + swift test landing on 123/16/0; ran a fresh regenerate-from-scratch and confirmed zero additional diff (codegen gate); confirmed finding 7's new assertion is real (the literal appears in actual generated output, not just the test's own fixture) and additive-only; ruled out swift format via git diff -w --stat matching plain git diff --stat line counts.

    One non-blocking observation, matching what I'd already flagged above: declarationsAreEmittedInSortedSchemaNameOrder's reverse map (built only from config.typeRenames) doesn't account for the new knownAcronyms renames, so for the 6 acronym-only-renamed names it sorts on the emitted spelling rather than the true original. It verified by hand that this still passes for a real structural reason today (the acronym casing never changes the word-initial capital, and every first-differing character between adjacent sorted names sits at a word boundary), not by luck -- but the guarantee isn't enforced by the test's own logic, so a future acronym or schema addition could pass coincidentally-sorted-but-wrong, or fail without explaining why. Not a regression from this change and not something this task asked to fix. Leaving as a latent gap; will card a follow-up rather than expand scope here.

    All 7 findings now checked off in the description. Task stays in `doing` per /implement -- ready for /review.
  timestamp: 2026-07-27T15:54:13.473785+00:00
- actor: claude-code
  id: 01kyj9nedez4v909h8kkgyst4v
  text: |-
    Implemented all 6 findings from the 2026-07-27 10:59 review round. Full resolution notes are inline in the description under that heading; this comment adds the finding-2 investigation detail the task specifically asked to record here.

    Finding-2 investigation (SessionConfigSelectOptions "never defined"): treated as suspect from the start, since it contradicts multiple prior rounds' clean 123/16/0 builds on this exact file. Did the grep before touching anything: `grep -rn "SessionConfigSelectOptions"` across the whole tree returns three reference sites in Models.generated.swift (SessionConfigSelect's property, initializer parameter, and decode call) plus one definition site — `public typealias SessionConfigSelectOptions = JSONValue` in Sources/FoundationModelsACP/Generated/Unresolved.generated.swift. That file is where this milestone's row-5 placeholder seam lives: eight deliberately-untyped definitions (three free-form extension escape hatches, five untagged unions with no discriminator to pin), named and pinned by VendoredSchemaTests.onlyTheDeliberatelyFreeFormDefinitionsStayUntyped. SessionConfigSelectOptions is one of them.

    Also checked the finding's own cited line (2850) directly: in the current 6278-line file that line sits inside MCPCapabilities.init(from:), nowhere near SessionConfigSelect. Same story for the other five findings this round — every cited line (1538, 2850, 2910, 3245, 6320, 6332) lands on unrelated code, which is consistent with the review having run against a revision before the immediately preceding round's acronym-casing regeneration shifted every line below its first edit. Ran a full `rm -rf .build && swift build --build-tests` plus `swift test` before and after this round's other changes: 123/16/0 both times, matching every prior round's report.

    Verdict: reviewer false positive from an incomplete search (checked only Models.generated.swift, missed the sibling Unresolved.generated.swift where placeholder typealiases live), compounded by stale line numbers. Not a latent bug the build tooling missed, and not something a definition needed inventing for — the type already exists under its correct name in the correct file. No generator or generated-output change was made for this finding.

    The other five findings were real and fixed in the generator, then regenerated (never hand-edited the generated files):

    Findings 1 and 3 (DiffChange.Payload, SessionConfigOption.Payload discriminator strings) share one emitter site, taggedUnionDeclaration. Added a private nested Tag: String enum per union, mapping each case name to its wire discriminator once; decode reads Tag(rawValue:), encode reads Tag.<case>.rawValue. Same site backs every stand-alone union in Unions.generated.swift and every object-nested Payload enum, so the fix landed everywhere in one regeneration.

    Finding 4 (SetSessionConfigOptionRequest.Value) is a different emitter site, the object-value-union family (valueUnionDecoder/valueUnionEncoder) — checked this explicitly rather than assuming the same fix applied. Added valueUnionTagDeclaration(cases:), emitted only when at least one variant pins a const tag, feeding the same Tag(rawValue:)/Tag.<case>.rawValue pattern. Split valueUnionDecoder into two smaller functions afterward for readability (pure extraction, verified byte-identical regeneration).

    Findings 5 and 6 (stale McpServer::Http / McpServer::Stdio doc prose) needed the prior round's applyKnownAcronymCasing extended to doc-comment text, since that function only ever touched emitted identifiers. Added description(of:) as the single read path for every one of ten call sites that used to read a schema fragment's description directly, and applyKnownAcronymCasingToProse(_:), which cases only capitalized identifier-like tokens in free text. That capitalization restriction mattered: an early version without it wrongly turned the schema's lowercase wire-path mention, session.mcp.http, into session.MCP.HTTP — caught by grepping the regenerated output for every remaining Mcp/Http occurrence, not by trusting the two cited sites were the whole story. Swept the full file afterward; no other stale-casing doc comments remain.

    Ran a fresh /review pass on the working diff after finishing (not requested by the task, done as a sanity check). It returned four findings, all either pre-existing patterns untouched by this round's diff (a repeated "case unknown(String, JSONValue)" literal and a repeated CodingKeys block, both shared between taggedUnionDeclaration and discriminatedUnionDeclaration since before this round) or pre-existing test issues already raised and explicitly ruled out of scope in an earlier round's review (the unused assertRoundTrips helper, and the orphaned "on that discriminator." doc-comment fragment in TaggedUnionTests.swift, both confirmed present in HEAD before this round touched anything). None are regressions from this round's changes; left as-is per the standing "no unrelated refactors" rule.

    Verified green: swift build --build-tests clean via full rm -rf .build rebuild; swift test = 123/16/0, 0 warnings, reproduced after each substantive change in this round. DocC --warnings-as-errors clean. checkedInOutputMatchesAFreshRun green, reconfirmed independently by copying the generated files aside, deleting .schema-hash, regenerating from scratch, and diffing byte-for-byte — a no-op after both the discriminator-table fix and the doc-casing fix. Updated one test, TaggedUnionTests.quoteBearingTagEscapesInEmittedSource, to assert the escaped tag against its new location (the Tag enum's raw value) instead of the old inlined switch-case literal, preserving the same escaping proof.

    Leaving in doing per /implement, ready for /review.
  timestamp: 2026-07-27T17:23:51.342573+00:00
- actor: claude-code
  id: 01kyjc1b7hc1jnrjynannt14xn
  text: |-
    Implemented all 4 findings from the 2026-07-27 12:29 review round. Full resolution notes are inline in the description under that heading; this comment adds the cross-cutting detail.

    Findings 1 and 2 are the same root fix: extracted a shared `tagEnumDeclaration(entries:rawType:baseIndent:)` helper in Emitter.swift and pointed `tagDeclaration` (flattened tagged unions), the new `discriminatedTagDeclaration` (discriminated anyOf unions), and `valueUnionTagDeclaration` (value unions) at it. `discriminatedUnionDeclaration`'s decode switch still switches over the optional String discriminator (so it keeps distinguishing an absent discriminator, which selects the default variant, from a present-but-unrecognized one, which routes to .unknown) but each tagged case now matches `case Tag.<case>.rawValue?:` instead of re-rendering `stringLiteral(tag)`; encode reads `Tag.<case>.rawValue` back.

    Finding 3 (the -32602 literal) was resolved by generalizing rather than adding one flat `invalidParamsCode` constant as literally suggested: ErrorCode repeats each of its eight wire constants across `wireValue` and `init(wireValue:)`, not just -32602, and every other scalar enum (PermissionOptionKind, PlanEntryPriority, StopReason, etc.) has the identical shape. Extended `scalarEnumDeclaration` to emit a nested `Tag` enum via the same shared helper (with a new `rawType` parameter so it serves `Int` as well as `String`), so every scalar enum's wire constants are each written once. Regenerating changed only Unions.generated.swift; Models.generated.swift, Identifiers.generated.swift, Unresolved.generated.swift, and .schema-hash diffed byte-identical, confirming `.discriminatedUnion` still matches zero vendored definitions and the scalar-enum change didn't touch any object-nested union.

    Finding 4: added the missing decode-arm assertion to `TaggedUnionTests.quoteBearingTagEscapesInEmittedSource` without touching the rest of the test.

    Updated two pre-existing tests whose assertions pinned the now-superseded inlined-literal shape: `UnknownFallbackEmissionTests.anyOfIntegerEnumWithOpenTailEmitsAnIntEnum` and `.quoteBearingWireValueEscapesInEmittedSource` in Tests/ACPGenerateTests/UnknownFallbackTests.swift, both updated to assert against the Tag enum instead of the old `case .parseError: -32700` / `case "..." : self = .aBC` shape (same treatment `TaggedUnionTests.quoteBearingTagEscapesInEmittedSource` got in the 10:59 round for the sibling family).

    Sweep for other inconsistently-applied instances of the pattern (requested explicitly, to avoid a 5th round): dispatched an independent read-only fork to re-check every remaining `stringLiteral(`/`wireLiteral(` call site in Emitter.swift and the two flat data-table renderers in SchemaGenerator.swift (`methodTableDeclaration`, `unstableNamespaceDeclaration`). Result: no further instances. `codingKeyCase` writes a wire-name literal exactly once (nothing re-renders it — every decode/encode site after it references the enum case, not the raw string), and the two method-table renderers are one-way tables with no reverse lookup to drift from.

    Also found and fixed a real defect in this card's own record, unrelated to the review findings but discovered while reading the description to plan the checkbox edits: the entire description body had been stored double-JSON-escaped (literal `\n` and `\"` two-character sequences instead of real line breaks and quotes) since some earlier round — confirmed via `.kanban/tasks/01KYD58WN4KFHR3TP9HQZHT036.md`, where the whole body was one 28,937-character physical line. This is why `progress` was reading `0.0` despite most checkboxes being checked: the checkbox counter never saw a line boundary. Decoded with Python's `json.loads` (wrapping the raw text in quotes) and rewrote the description with real newlines in the same update that checked off this round's four boxes; no wording changed beyond that and the four resolutions. `progress` now reads 0.9722222222222222 (35 of 36 checkboxes — the one intentionally-open `_meta` three-state item).

    Verification (fresh, this round): `swift build --build-tests` clean via a full `rm -rf .build` rebuild; `swift test` = 123 tests / 16 suites / 0 failures (52 FoundationModelsACPTests + 71 ACPGenerateTests), matching the standing baseline exactly. 0 warnings. DocC (`swift package generate-documentation --target FoundationModelsACP --warnings-as-errors`) clean. `checkedInOutputMatchesAFreshRun` (codegen diff gate) green. No `swift format` run. `git diff --stat` shows exactly the expected files: Sources/ACPGenerateCore/Emitter.swift, Sources/FoundationModelsACP/Generated/Unions.generated.swift, Tests/ACPGenerateTests/TaggedUnionTests.swift, Tests/ACPGenerateTests/UnknownFallbackTests.swift.

    Leaving in `doing` per /implement — ready for /review.
  timestamp: 2026-07-27T18:05:18.449933+00:00
- actor: claude-code
  id: 01kyjcezeqc58egd624j5gaejd
  text: |-
    Adversarial double-check (independent agent, fresh evidence) on this round's diff returned REVISE with one finding, acted on below (bounded to one re-check per really-done's contract).

    Finding: finding #1 (discriminatedUnionDeclaration's new Tag-enum treatment) had no test pinning it, unlike findings #2-#4 which each landed with a corresponding assertion update. The vendored v2 schema has zero discriminated-union definitions, and `checkedInOutputMatchesAFreshRun`'s no-op diff never exercises this code path either, so the fix's correctness rested solely on manual reading, not a regression test.

    Fixed: added assertions to the existing `AnyOfUnionTests.discriminatedUnionClassifiesAndEmits` (whose fixture already has exactly the right shape — one tagged case `alpha` plus a discriminator-less default `beta`, so no new fixture was needed) pinning `private enum Tag: String {`, `case alpha = "alpha"`, `case Tag.alpha.rawValue?:` (decode), and `try container.encode(Tag.alpha.rawValue, forKey: .type)` (encode). Ran it in isolation first to confirm the exact emitted strings match byte-for-byte, then the full suite.

    The double-check's other checks all passed independently: the three-way absent/known/unrecognized decode split is intact and backed by the pre-existing `guard defaultCount == 1` invariant; `tagEnumDeclaration` is called from exactly the four claimed sites with no leftover inlined-literal duplicate; regeneration is a no-op against the working tree; all four (now five) test-file diffs are additive/non-weakening; the independent sweep for other duplication instances holds; and the kanban description repair reads coherently.

    Re-verified green after the fix: `swift build --build-tests` clean via full `rm -rf .build` rebuild; `swift test` = 123 tests / 16 suites / 0 failures (52 + 71, unchanged count since this added assertions to an existing test rather than a new one). `git diff --stat` now shows five source/test files plus the kanban mirror: Emitter.swift, Unions.generated.swift, AnyOfUnionTests.swift, TaggedUnionTests.swift, UnknownFallbackTests.swift.

    Task is green and ready for /review, staying in `doing`.
  timestamp: 2026-07-27T18:12:45.143570+00:00
depends_on:
- 01KYD58WKPK64VW7RWG16B89QC
position_column: done
position_ordinal: '8180'
title: M1 Types and conventions, enforced at decode time
---
## Starting point

**This is a rewrite.** `plan.md` -> *Starting point* has the full inventory.

**Survives from v1, do not rewrite:** `Core/JSONValue.swift`, `Core/AbsolutePath.swift`, `Core/MethodInfo.swift`, `Core/WireRawValueCodable.swift`. These are v2-agnostic and the generator itself imports them. `AbsolutePath` already rejects relative paths at decode time.

**Already restored in M0:** `Core/ProtocolVersion.swift` (now carrying `.v2`) and `Core/ForgivingDecoding.swift` — the regenerated v2 output references them and would not compile otherwise. The checked-in generated output **compiles today**; this task no longer opens with a make-it-build step.

**Deleted, and yours to rebuild:** `Core/ACP.swift` (the namespace anchor) and `Connection/RequestError.swift`. Their v1 implementations are in git history and were sound; recovering and re-checking them against v2 is legitimate and faster than reinventing.

**`Core/LineNumber.swift` is deliberately *not* restored** — see *Conventions* below. Nothing in the v2 output references it.

## What

`plan.md` -> **M1** and **Conventions the type system should enforce**.

Generated models, unions, and enums from the v2 schema, plus the hand-written pieces that are deliberately never generated: `JSONValue`, `AbsolutePath`, `RequestError`, and the `unknown(String)` fallbacks.

**Conventions are type-system obligations, not documentation:**

- **Absolute paths everywhere.** An `AbsolutePath` newtype turns a relative path into a decode-time error rather than a runtime surprise three layers up. v2 gives absolute paths a first-class `AbsolutePath` schema `$def`, so the invariant rides each field's own `$ref` and the generator needs no per-field override table — which is why `GeneratorConfig.acpV2.wireInvariantFields` is **empty by design**, and `AbsolutePath` is simply listed as hand-written. A per-field table would only be a second, drift-prone copy of the schema.
- **No line-number invariant. This is a deliberate divergence from v1, not an oversight.** v1 stated 1-based line numbers and enforced them through a hand-written `LineNumber` type and a `.lineNumber` entry in `wireInvariantFields`. **v2 states no such thing.** The vendored schema has exactly one line-valued field, `ToolCallLocation.line`, described only as *"Optional line number within the file"* — and it carries `minimum: 0`, so the schema explicitly permits `0`. The v2 tool-calls page does not state a base either. Enforcing 1-based would therefore **reject payloads v2 permits**. The generator keeps the override mechanism (still covered by `GeneratorCoreTests`) for a revision that states the invariant in prose alone, but nothing uses it today. Do not restore `LineNumber`, and do not add a `.lineNumber` mapping, until a re-vendor states the invariant.
- `camelCase` object keys, `snake_case` discriminator values.
- **Unknown values are accepted and preserved.** Unknown enum and tagged-union cases must round-trip intact when proxying -- v2 states this explicitly, and it is what allows a newer peer to talk to us without data loss. Values beginning with `_` are implementation-specific; unknown non-underscore values are reserved for future versions.
- `_meta` follows patch semantics in updates, scoped to its parent object.

## The placeholder seam M0 left for this task

Four union shapes currently defer to raw `JSONValue` rather than truncate what their catch-all carries, untyping fourteen properties in all — including `InitializeResponse.authMethods`, `PlanUpdate.plan`, `ResumeSessionRequest.replayFrom`, and the entire `session/set_config_option` params object. **`plan.md` -> *Milestones* -> M1 carries the full instructions**, including which one to take first (`SetSessionConfigOptionRequest`), the three coupled `SchemaGenerator.swift` edits it needs, and the round-trip test that must land in the same change. M2, M5, and M7 each note the surface this seam untypes for them; resolving it here is what unblocks their typed payloads.

## Test coverage to re-establish

The reset dropped these suites along with the v1 schema. They were real coverage, not ceremony — recreate them against v2 rather than letting them lapse:

- **`ForgivingDecodingTests`** — the forgiving-decode helpers. `ForgivingDecoding.swift` itself came back in M0; its tests did not.
- **`TaggedUnionRoundTripTests`** — runtime decode/encode of every generated tagged-union variant against wire fixtures: decode picks the right case, re-encoding is byte-equivalent modulo key order.
- **`UnknownFallbackRoundTripTests`** — runtime proof that unrecognized string-enum values and union discriminators decode to `.unknown` and re-encode their captured string.

## Acceptance Criteria

- [x] `ACP` and `RequestError` restored; the full v2 generated output still compiles.
- [x] Generated types cover the full v2 schema.
- [x] `AbsolutePath` rejects relative paths at decode time.
- [x] `ToolCallLocation.line` accepts `0` — no 1-based invariant is enforced anywhere.
- [x] Every enum and tagged union has an `unknown(String)` (or equivalent) fallback.
- [x] Decode -> encode round-trips preserve unknown cases and `_`-prefixed extensions byte-for-byte in content.
- [x] `RequestError` carries structured `data`; no JSON smuggled through the message string.
- [x] The three dropped suites above exist again, against v2 types.

## Tests

- [x] A relative path fails decoding with a clear error.
- [x] `ToolCallLocation` decodes with `line: 0`, with `line` omitted, and with `line: null` — the v1 test that required `0` to *fail* must not be recreated.
- [x] A payload with an unrecognized enum case round-trips without loss.
- [x] A payload with a `_customThing` extension round-trips without loss.
- [ ] `_meta` patch semantics: omitted vs null vs value behave per spec. **Partly.** Where the schema says omitted and `null` are equivalent, they are, and `nil` never encodes as an explicit null. Six upsert `_meta` fields say `null` is a distinct clear signal, which a Swift `Optional` cannot express; carded as ^1pfngj1 for M7 and pinned in its current form by `MetaFieldTests.upsertMetaCannotYetDistinguishOmittedFromNull`.
- [x] `JSONValue` round-trips all six kinds including nested containers.

## Review Findings (2026-07-26 15:39)

- [x] `Sources/ACPGenerateCore/Emitter.swift:752` — Value unions with untagged defaults silently lose unrecognized discriminators on re-encode, while the PR's goal (visible in tagged and discriminated unions) is to preserve them with synthesized `.unknown` cases. The docstring claims untagged defaults handle 'absent or unknown' discriminators, but the generated code doesn't capture unknown ones. Either (1) add schema validation to reject value unions with untagged defaults when tagged cases exist (since untagged variants cannot properly preserve unrecognized tags), or (2) clarify the docstring to specify that untagged defaults only handle 'absent' (nil) discriminators, not unrecognized values, or (3) ensure value union defaults are always `.capturedTag` when tagged cases exist, consistent with the union-wide pattern of preserving unknown variants. **Resolved via option 2.** Rejecting the shape (option 1) was ruled out: `objectValueUnionSchema`/`objectValueUnionFiltersTheBareUnknownFallback` already exercise exactly this shape (a tagged case beside a discriminator-less default) as the primary documented value-union fixture, so rejecting it breaks the feature it tests. Forcing `.capturedTag` (option 3) was ruled out too: it would require strict `container.decode` for a variant whose own schema declares no discriminator at all, turning today's valid "absent" decode into a decode error — a regression, not a fix. The docstring in `valueUnionDecoder` now states plainly, for the untagged-default case, that the default variant's own schema places no constraint on the member, so a present-but-unrecognized value matches it same as an absent one, and — having no discriminator slot of its own — does not round-trip. That is what the code has always done; only the doc overclaimed. The `.capturedTag` docstring branch was tightened too, to say it round-trips.
- [x] `Sources/ACPGenerateCore/Emitter.swift:1062` — Parameter-listing code with comma handling is duplicated across two functions. The loop pattern for enumerating parameters, building commas, and appending indented lines appears in both `memberwiseInit` (lines 182–189) and `unionStructInit` (lines 1062–1066), differing only in input type and variable names. Factor the parameter-listing logic into a generic helper function (e.g., `renderParameters(items:render:)` accepting an array and a render closure). Refactor both `memberwiseInit` and `unionStructInit` to call this helper, passing `{ initParameter($0) }` or `{ $0 }` respectively. **Resolved as specified** — added `renderParameters(items:render:)` and both call sites now use it. Generated output unaffected (pure emitter-internal refactor).
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:1349` — The array `["operation", "fileType", "mimeType", "_meta"]` is a repeated literal that appears again at line 1364. Repeated literals should be named constants to avoid drift when maintaining parallel code paths (decode and encode). Define a private static constant within the Payload enum: `private static let excludedMembers = ["operation", "fileType", "mimeType", "_meta"]` and reference it in both methods. **Resolved in the emitter, then regenerated.** Added `Emitter.excludedMembersDeclaration(discriminator:siblingMembers:)`, emitted once per union (right after `CodingKeys`), and pointed `unknownDecodeArm`/`unknownEncodeArm` at `Self.excludedMembers` instead of re-rendering the literal. (Bare `excludedMembers` doesn't compile from instance-method scope in Swift — confirmed by a minimal repro — hence `Self.`.) Regeneration is otherwise byte-identical; `checkedInOutputMatchesAFreshRun` stays green.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:1364` — The array `["operation", "fileType", "mimeType", "_meta"]` is a duplicate of line 1349. This repeated literal should be extracted to a named constant. Extract to a private static constant and use it in both init and encode methods. **Resolved together with the :1349 finding** — same emitter change, same constant.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:3029` — The array `["type", "configId", "name", "category", "description", "_meta"]` is a duplicate of line 3014. This repeated literal should be extracted to a named constant. Extract to a private static constant and use it in both init and encode methods. **Resolved by the same emitter change** — `excludedMembersDeclaration` is called uniformly from both `taggedUnionDeclaration` and `discriminatedUnionDeclaration`, so this and every other stand-alone or object-nested tagged/discriminated union in the generated output (11 stand-alone unions in `Unions.generated.swift` plus the object-nested `Payload` enums in `Models.generated.swift`) picked up the constant in the same regeneration. Updated `AnyOfUnionTests.objectCarryingATaggedPayloadUnionEmitsANestedPayload` to assert the new constant + `Self.excludedMembers` reference instead of the old inlined literal it was pinning.

## Review Findings (2026-07-27 10:12)

- [x] `Sources/ACPGenerateCore/Emitter.swift:93` — The first line of the doc comment does not end with a period, violating the rule that the first line must be a single-sentence summary ending in a period. Rewrite the first line to be complete and end with a period: `/// Renders a parenthesized parameter list's body: one item per line, indented one level, comma-terminated except the last.`. **Resolved as specified** — merged the wrapped two-line first sentence into one period-terminated line.
- [x] `Sources/ACPGenerateCore/Emitter.swift:177` — The first line of the doc comment does not end with a period, violating the rule that the first line must be a single-sentence summary ending in a period. Rewrite the first line to be complete: `/// Renders the array literal of member names an unrecognized variant's payload must not hold, because something else encodes them.`. **Resolved as specified**, same merge treatment, on `ownedMembers`'s doc comment.
- [x] `Sources/ACPGenerateCore/Emitter.swift:185` — The first line of the doc comment does not end with a period, violating the rule that the first line must be a single-sentence summary ending in a period. Rewrite the first line to be complete: `/// Renders the private constant naming the members an unrecognized variant's payload must not hold.`. **Resolved as specified**, on `excludedMembersDeclaration`'s doc comment.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:2147` — Acronym 'HTTP' must be uniformly cased; in UpperCamelCase it must be all-caps, not mixed-case 'Http'. The casing rule explicitly lists 'Http' as wrong. Rename struct from 'McpServerHttp' to 'McpServerHTTP' and update all references throughout the file. **Resolved in the generator, not by hand-editing generated output.** Added `GeneratorConfig.knownAcronyms: Set<String>` (`["MCP", "HTTP"]` for `.acpV2`) and `SchemaGenerator.applyKnownAcronymCasing(to:)`, wired into `emittedName(name:)`: splits a PascalCase name at capital-letter boundaries and uppercases any word matching a known acronym case-insensitively, so the fix is uniform rather than a per-name literal. Derived the full affected set from `Schema/acp-v2.json`'s `$defs` keys rather than trusting the three cited call sites: **seven** definitions match the Mcp/Http pattern, not three — `McpServer`, `McpCapabilities`, `McpHttpCapabilities`, `McpServerHttp`, `McpServerStdio`, `McpStdioCapabilities`, `HttpHeader`. Regenerating (`rm .schema-hash && swift run acp-generate`) turned all seven into `MCPServer`, `MCPCapabilities`, `MCPHTTPCapabilities`, `MCPServerHTTP`, `MCPServerStdio`, `MCPStdioCapabilities`, `HTTPHeader`. Note this struct became `MCPServerHTTP` (Mcp also fixed), not the literally-cited `McpServerHTTP` — the uniform rule corrects every acronym in the name, and finding 2360 already required fixing Mcp on its sibling name, so leaving it mixed-case here would have been inconsistent, not merely different from the suggestion.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:2234` — Acronym 'HTTP' must be uniformly cased; in UpperCamelCase it must be all-caps, not mixed-case 'Http'. The casing rule explicitly lists 'Http' as wrong. Rename struct from 'McpHttpCapabilities' to 'McpHTTPCapabilities' and update all references throughout the file. **Resolved by the same generator change** — became `MCPHTTPCapabilities` (Mcp also fixed, per the uniform rule; see 2147's note).
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:2360` — Acronym 'MCP' must be uniformly cased; in UpperCamelCase it must be all-caps 'MCP', not mixed-case 'Mcp'. The casing rule applies uniformly to all acronyms. Rename struct from 'McpServerStdio' to 'MCPServerStdio' and update all references. **Resolved by the same generator change** — became `MCPServerStdio`, matching the finding exactly (Stdio is not a recognized acronym, so it stays as-is).
- [x] `Tests/ACPGenerateTests/AnyOfUnionTests.swift:170` — The test verifies the decode arm uses the deduped `excludedMembers` constant, but does not verify the encode arm also uses it. The comment explicitly states both directions should use the shared constant, but only the decode direction is asserted. Add an assertion to verify the encode direction also uses the constant: `#expect(models.contains("encodeMembers(to: encoder, reserving: Self.excludedMembers)"))`. **Resolved as specified** — added the exact assertion alongside the existing decode-arm one, without altering other assertions.

Updated hand-written references to the real renamed type (`McpServer` -> `MCPServer`) in `Tests/ACPGenerateTests/VendoredSchemaTests.swift` (two string-literal pins) and `Tests/FoundationModelsACPTests/TaggedUnionRoundTripTests.swift` (two `.self` type references). `Tests/ACPGenerateTests/GeneratorCoreTests.swift`'s inline `"McpServer"` schema fixture is deliberately untouched — it generates through its own `miniatureConfig`, which never sets `knownAcronyms`, confirmed by the suite staying green.

Verified green: `swift build --build-tests` clean; `swift test` = 123 tests / 16 suites / 0 failures (52 FoundationModelsACPTests + 71 ACPGenerateTests, matching the pre-change baseline), reproduced via a full clean rebuild. DocC (`--warnings-as-errors`) clean. `checkedInOutputMatchesAFreshRun` (codegen diff gate) green — a fresh regenerate is a byte-for-byte no-op against the committed output. Adversarial `double-check` returned **PASS**; see kanban comments for its one non-blocking observation (a latent gap in `declarationsAreEmittedInSortedSchemaNameOrder`'s sort-order reconstruction, not a defect in this change).

## Review Findings (2026-07-27 10:59)

- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:1538` — Discriminator strings "add", "delete", "modify", "move", "copy" are hardcoded and repeated in both decode (switch case) and encode (switch case) methods of DiffChange.Payload. Define these discriminators once as a table or mapping to avoid duplication and reduce the risk of encode/decode mismatch. Define a constant mapping of discriminators at the enum level, e.g. `let discriminatorMap: [String: String] = ["add": "add", "delete": "delete", ...]` or use a lookup table to eliminate hardcoded string repetition in both methods. **Resolved in the emitter, then regenerated.** `taggedUnionDeclaration` now emits a private nested `Tag: String` enum mapping each case name to its wire discriminator once; decode looks it up via `Tag(rawValue:)` and encode reads `Tag.<case>.rawValue` back — the same pattern class as `excludedMembers` (the prior round's fix for the analogous member-list duplication), a single named table both directions read instead of a per-arm literal. This is the same emitter site as the SessionConfigOption.Payload finding below (`taggedUnionDeclaration`, shared by every stand-alone union in `Unions.generated.swift` and every object-nested `Payload` enum), so all of them picked up the fix in one regeneration; verified by checking each, not assumed.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:2850` — The type `SessionConfigSelectOptions` is referenced in the `SessionConfigSelect` struct (as a property type, initializer parameter, and in the `Codable` conformance methods) but is never defined in this file. This would cause a compilation error when the struct tries to use it. Define the `SessionConfigSelectOptions` type in this file. Based on the context (a property named `options`, a set of selectable options), it appears this should be either: (1) a struct/enum that contains the options, or (2) replaced with `[SessionConfigSelectOption]` if it should be an array of individual options. The code generator needs to be fixed to emit this definition. **False positive — no fix applied.** Verified before touching anything else, per instruction: `grep -rn "SessionConfigSelectOptions"` across the tree finds the type defined at `Sources/FoundationModelsACP/Generated/Unresolved.generated.swift` as `public typealias SessionConfigSelectOptions = JSONValue` — one of the deliberately-untyped row-5 placeholder-seam definitions this milestone leaves deferred on purpose (pinned by name in `VendoredSchemaTests.onlyTheDeliberatelyFreeFormDefinitionsStayUntyped`). The reviewer's own cited line (2850, and every other line this round cites) lands inside `MCPCapabilities.init(from:)` in the current file, nowhere near `SessionConfigSelect` — the finding's line numbers are stale against the current file, most likely written against an earlier revision before the acronym-casing regeneration shifted every subsequent line. A clean `swift build --build-tests` and `swift test` (123/16/0, reproduced via full clean rebuild both before and after this round's other fixes) confirm the file compiles, consistent with every prior round's report. Conclusion: reviewer false positive — the type exists under its correct name, just in a sibling generated file the reviewer didn't check — not a bug the build tooling missed. No generator or generated-output change was made for this finding.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:2910` — Discriminator strings "select" and "boolean" are hardcoded and repeated in both decode (line ~2910) and encode (line ~2930) methods of SessionConfigOption.Payload. Each discriminator appears twice; define once to maintain single source of truth. Extract discriminator strings into enum-level constants or a private dictionary mapping case names to their discriminator strings, used by both decode and encode. **Resolved by the same emitter change as the DiffChange.Payload finding above** — same `taggedUnionDeclaration` site, same `Tag: String` table pattern.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:3245` — Discriminator strings "id" and "boolean" are hardcoded and repeated in both decode (line ~3245) and encode (line ~3265) methods of SetSessionConfigOptionRequest.Value. Each string literal appears twice; consolidate to one definition. Define discriminator strings as enum-level constants or a private static mapping table referenced by both decode and encode methods to eliminate duplication. **Resolved at a different emitter site — checked, not assumed.** This is the object-value-union family (`valueUnionDecoder`/`valueUnionEncoder`), not `taggedUnionDeclaration`; read both before picking a fix rather than reusing the DiffChange one blind. Added `valueUnionTagDeclaration(cases:)`, emitted only when at least one variant pins a `const` tag (an all-default value union has no tag to deduplicate), and pointed both directions at the same `Tag(rawValue:)` / `Tag.<case>.rawValue` pattern used for the flattened tagged-union family. The lone `.capturedTag` default (`.other`) still writes its own captured discriminator directly, since it has no fixed tag to look up — that path is unchanged. `valueUnionDecoder` was then split into `valueUnionDecoderBody`/`valueUnionDecoderBodyWithoutTaggedCases` to keep the tagged/untagged branches independently readable, a pure extraction verified byte-identical before and after regeneration.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:6320` — Doc comment references `McpServer::Http` with lowercase 'Mcp', but the change renamed all types to use all-caps acronyms (e.g., MCP, HTTP). The reference should use the same all-caps naming as the renamed types elsewhere in the file. Update the doc comment to refer to the correctly-cased type name, e.g., `MCPServer::HTTP` or `MCPServer::Http` depending on the actual union variant name in Unions.generated.swift. **Resolved in the generator, not by hand-editing generated output.** The doc-comment text comes verbatim from the schema's `description` field, read at ten call sites across `SchemaGenerator.swift` — none of them ran the text through the prior round's `applyKnownAcronymCasing`, which only ever touched emitted *identifiers*, never doc prose. Added `description(of:)` as the one read path for all ten call sites, and `applyKnownAcronymCasingToProse(_:)`, which tokenizes free text on non-identifier characters and cases each *capitalized* token the same way `applyKnownAcronymCasing` cases a whole type name. Restricted to capitalized tokens deliberately, and only after catching a real regression: the schema also quotes a lowercase dotted wire-path, `session.mcp.http` (an actual JSON capability key, not a type reference), and a first blanket version of this fix wrongly turned that into `session.MCP.HTTP` before the capitalization guard went in — caught by grepping the regenerated output for every remaining `Mcp`/`Http` occurrence rather than trusting the two cited sites were the whole story. Regenerated text now reads `Agent supports [`MCPServer::HTTP`].`.
- [x] `Sources/FoundationModelsACP/Generated/Models.generated.swift:6332` — Doc comment references `McpServer::Stdio` with lowercase 'Mcp', but the change renamed all types to use all-caps acronyms. The reference should use the same all-caps naming as the renamed types elsewhere in the file. Update the doc comment to refer to the correctly-cased type name, e.g., `MCPServer::Stdio` (with uppercase MCP). **Resolved by the same generator change** — regenerated text now reads `Agent supports [`MCPServer::Stdio`].` (`Stdio` stays as-is; it is not a recognized acronym, matching the prior round's identifier-casing precedent). Swept the full regenerated output for every other `Mcp`/`Http` mention beyond these two cited lines, per the task's instruction to check: none remain outside the (correctly untouched) lowercase `session.mcp.http`/`session.mcp.stdio` wire-path mentions.

All six findings' cited line numbers (1538 through 6332) are stale against the current 6278-line file — every one lands on unrelated code when checked directly, most likely because the review that produced them ran against a revision before the prior round's acronym-casing regeneration shifted everything below it. Each finding was instead located by searching for the described symptom (the struct/enum name and the described defect), not by trusting the cited line number.

Verified green: `swift build --build-tests` clean via a full `rm -rf .build` rebuild; `swift test` = 123 tests / 16 suites / 0 failures (52 FoundationModelsACPTests + 71 ACPGenerateTests), reproduced after every substantive change in this round, not just once at the end. 0 warnings in either the build or test log. DocC (`--warnings-as-errors`) clean. `checkedInOutputMatchesAFreshRun` green, and independently reconfirmed by copying the generated files aside, deleting `.schema-hash`, regenerating from scratch, and diffing byte-for-byte against the copy — a no-op both after the discriminator-table fix and again after the doc-casing fix. One updated test: `TaggedUnionTests.quoteBearingTagEscapesInEmittedSource` was pinned to the old inlined-literal decode/encode text; updated to assert the escaped tag now lives once in the `Tag` enum's raw value and that encode reads `Tag.aBC.rawValue`, keeping the same escaping proof (quote and backslash cannot break out of the literal) against the new shape.

## Review Findings (2026-07-27 12:29)

- [x] `Sources/ACPGenerateCore/Emitter.swift:776` — The PR deduplicates tag string literals in taggedUnionDeclaration and valueUnionEnum by introducing Tag enums and using Tag(rawValue:) in decode and Tag.name.rawValue in encode. However, discriminatedUnionDeclaration still repeats stringLiteral(tag) directly in both decode and encode paths, inconsistently handling the same sentinel value across three union families. This creates maintenance fragility: future modifications to discriminated unions risk desynchronization if the pattern isn't applied uniformly. Apply the same deduplication pattern to discriminatedUnionDeclaration: create a Tag enum via tagDeclaration(), then use Tag(rawValue:) in the decode switch and Tag.name.rawValue in the encode calls, matching the invariant applied to taggedUnionDeclaration and valueUnionEnum. **Resolved in the emitter, then regenerated.** Added `discriminatedTagDeclaration(cases:)`, mirroring `tagDeclaration`/`valueUnionTagDeclaration`: a `Tag: String` entry for every case with a `const` tag, none for the discriminator-less default. Decode now matches `case Tag.<case>.rawValue?:` instead of `case \(stringLiteral(tag))?:`, which still correctly separates an *absent* discriminator (`case nil:`, selects the default) from a *present-but-unrecognized* one (routes to `.unknown`) — a plain `Tag(rawValue:)` lookup over the whole optional would have collapsed those two, so the fix keeps the outer `String?` switch and only replaces the literal each case matches against. Encode reads `Tag.<case>.rawValue` back. Regeneration touched only `Unions.generated.swift`; `.discriminatedUnion` matches zero vendored definitions (only synthetic generator-test schemas reach it), so `Models.generated.swift` is unaffected.
- [x] `Sources/ACPGenerateCore/Emitter.swift:1013` — `valueUnionTagDeclaration` reimplements the enum-rendering pattern already implemented by `tagDeclaration` (line 573), duplicating the logic of building a `private enum Tag: String` with case-to-string-literal mappings. Extract `tagEnumDeclaration(entries:baseIndent:)` helper accepting `[(swiftName: String, tag: String)]` and indentation level, returning formatted lines. Have `valueUnionTagDeclaration` call this helper after pre-filtering its tagged entries. **Resolved as specified, generalized one step further.** Added `tagEnumDeclaration(entries:rawType:baseIndent:)` (an extra `rawType` parameter beyond the suggestion, so the same renderer also serves an integer-keyed raw type — see the :273 finding) and pointed `tagDeclaration`, the new `discriminatedTagDeclaration`, and `valueUnionTagDeclaration` at it. All three Tag-enum families now share one renderer instead of three near-identical loops.
- [x] `Sources/FoundationModelsACP/Generated/Unions.generated.swift:273` — Hardcoded status code -32602 (JSON-RPC invalidParams) should be a named constant. Extract to a named constant: private static let invalidParamsCode = -32602. **Resolved in the emitter, then regenerated — generalized rather than one flat constant.** `ErrorCode` repeats each of its *eight* wire constants across `wireValue` and `init(wireValue:)`, not just `-32602`; a lone `invalidParamsCode` constant would have fixed one of eight and left the other seven (and every other scalar enum's wire constants, e.g. `PermissionOptionKind`, `PlanEntryPriority`) exactly as duplicated — the same "fixed the cited site, not the pattern" gap the 10:59 and 10:12 rounds were reopened over. Extended `scalarEnumDeclaration` to emit a nested `Tag` enum via the same `tagEnumDeclaration` helper above (`rawType: Int` for `ErrorCode`, `String` for the rest), so `-32602` and every sibling constant is written once, as `Tag.invalidParams.rawValue` etc., read back by both `wireValue` and `init(wireValue:)`. Regenerating changed only `Unions.generated.swift` (every stand-alone scalar enum picked up it in one pass); `Models.generated.swift`, `Identifiers.generated.swift`, `Unresolved.generated.swift`, and `MethodTable.generated.swift` diffed byte-identical. Updated `UnknownFallbackEmissionTests.anyOfIntegerEnumWithOpenTailEmitsAnIntEnum` and `.quoteBearingWireValueEscapesInEmittedSource`, which pinned the old inlined-literal shape.
- [x] `Tests/ACPGenerateTests/TaggedUnionTests.swift:80` — Test claims both encode and decode use the Tag enum per its comment ('both decode (`Tag(rawValue:)`) and encode (`Tag.aBC.rawValue`) read that one literal back') but only asserts the encode direction; the symmetric decode assertion is missing. Add an assertion to verify the decode path uses the Tag enum symmetrically, e.g., `#expect(source.contains("switch Tag(rawValue: discriminator)"))` or a pattern that confirms Tag(rawValue:) is invoked in the decode branch. **Resolved as specified** — added `#expect(source.contains("switch Tag(rawValue: discriminator) {"))` alongside the existing encode-arm assertion, leaving the rest of the test (and the shared `assertRoundTrips`/`canonicalized` helpers) untouched.

Independent sweep (a forked read-only agent, not this round's own author) re-checked every other `stringLiteral(`/`wireLiteral(` call site in `Emitter.swift` and the two flat data-table renderers in `SchemaGenerator.swift` (`methodTableDeclaration`, `unstableNamespaceDeclaration`) for the same forward/reverse-duplication shape. None qualify: `codingKeyCase` writes a wire-name literal exactly once (every decode/encode site after it references the enum case, never the raw string again), and the two method-table renderers are one-way tables with no reverse lookup to drift from. No fifth round is needed for this pattern class.

Verified green: `swift build --build-tests` clean via a full `rm -rf .build` rebuild; `swift test` = **123 tests / 16 suites / 0 failures** (52 FoundationModelsACPTests + 71 ACPGenerateTests), matching the standing baseline exactly — this round's `discriminatedUnionDeclaration`/`scalarEnumDeclaration` changes did not shift the count. 0 warnings in build or test output. DocC (`--warnings-as-errors`) clean. `checkedInOutputMatchesAFreshRun` (the codegen diff gate) green: regenerating from the vendored schema after the emitter change reproduces exactly `Unions.generated.swift`'s new form and leaves `Models.generated.swift`, `Identifiers.generated.swift`, `Unresolved.generated.swift`, and `.schema-hash` untouched relative to what's committed.

Also repaired this card's own description: the entire body had been stored double-JSON-escaped (literal `\n`/`\"` text instead of real line breaks and quotes) since an earlier round, which is why `progress` was reading `0.0` despite 31 of the (now) 35 checkboxes being checked — the checkbox counter never saw a line boundary because the whole body was one 28,937-character line. Decoded and rewritten with real newlines in this update; no wording changed beyond that and the four checkboxes above.
