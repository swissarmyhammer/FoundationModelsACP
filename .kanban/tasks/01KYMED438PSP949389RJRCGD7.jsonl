{"id":"01KYMED439CW56J96DSVGA4HA8","timestamp":"2026-07-28T13:25:10.377034Z","op":"create","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,35 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: 8f80\n+title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n+---\n+## Why this card exists\n+\n+Surfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\n+\n+## The gap\n+\n+`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\n+\n+```swift\n+guard fallbacksAreGenuineCatchAlls(of: variants) else {\n+    return .deferredUnion(keyword: Self.oneOfKey)\n+}\n+return .taggedUnion\n+```\n+\n+It unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \"reached less directly\" pattern the sibling task just closed for `anyOf`'s object branch.\n+\n+This is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\n+\n+## Suggested fix\n+\n+Mirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\n+\n+## Acceptance criteria\n+\n+- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\n+- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\n+- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,35 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: 8f80\n-title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n----\n-## Why this card exists\n-\n-Surfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\n-\n-## The gap\n-\n-`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\n-\n-```swift\n-guard fallbacksAreGenuineCatchAlls(of: variants) else {\n-    return .deferredUnion(keyword: Self.oneOfKey)\n-}\n-return .taggedUnion\n-```\n-\n-It unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \"reached less directly\" pattern the sibling task just closed for `anyOf`'s object branch.\n-\n-This is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\n-\n-## Suggested fix\n-\n-Mirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\n-\n-## Acceptance criteria\n-\n-- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\n-- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\n-- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN1BGGP5DEZ88X7YJQBT0AZ","timestamp":"2026-07-28T18:56:20.502494Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: 8f80\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: 8f80\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\n","transaction_id":null}
{"id":"01KYN20CR6HFRD5PZA76YE8PRX","timestamp":"2026-07-28T19:07:44.774027Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,18 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01kyn20cr5z604mb2g86gycf5h\n+  text: |-\n+    Implemented: classifyOneOf now computes modeled = variants.filter { $0[notKey] == nil } after the fallbacksAreGenuineCatchAlls guard passes, and throws GeneratorError.unsupportedShape(context: name, detail: emptyUnionDetail) directly when modeled.isEmpty, instead of falling through to .taggedUnion and relying on taggedUnionModel's own downstream guard. Mirrors classifyAnyOf's object-branch fix exactly. Docstring's Throws clause updated to document the new case.\n+\n+    Mutation testing finding (important, differs from the anyOf sibling): reverted the guard, then tried the anyOf sibling's exact trick (a deliberately-broken sibling property alongside the oneOf, e.g. \"corrupt\": {\"type\": 1}) to see if the fix is externally observable. It is NOT — with the guard removed, the error is byte-identical: \"unsupported schema shape at OnlyFallback: empty union\". Root cause: classify() short-circuits on the oneOf key before ever looking at sibling type/properties (unlike classifyAnyOf's object branch, which only fires when members[typeKey]==object). And critically, classifyOneOf's .taggedUnion path calls taggedUnionModel directly with no other processing in between - unlike .objectTaggedUnion, whose objectTaggedUnionModel calls structModel (processing sibling members) before ever re-deriving the union. Since both the classification-site throw and taggedUnionModel's downstream throw use the identical context/detail pair, there is no schema construction that can make classifyOneOf's guard produce an observably different result. Restored the guard afterward; confirmed the removed-experiment test file has zero diff vs HEAD.\n+\n+    Given this, I did not add a new compound regression test - doing so would just be another no-op pinning test duplicating the existing oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail, which the acceptance criteria explicitly said to avoid. The existing test still pins the (unchanged) final error. The fix itself is documented in-code as a directness/proximate-cause improvement only, confirmed by mutation testing, not an externally-observable behavior change.\n+\n+    Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n+  timestamp: 2026-07-28T19:07:44.773652+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","reverse_patch":"--- original\n+++ modified\n@@ -1,18 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01kyn20cr5z604mb2g86gycf5h\n-  text: |-\n-    Implemented: classifyOneOf now computes modeled = variants.filter { $0[notKey] == nil } after the fallbacksAreGenuineCatchAlls guard passes, and throws GeneratorError.unsupportedShape(context: name, detail: emptyUnionDetail) directly when modeled.isEmpty, instead of falling through to .taggedUnion and relying on taggedUnionModel's own downstream guard. Mirrors classifyAnyOf's object-branch fix exactly. Docstring's Throws clause updated to document the new case.\n-\n-    Mutation testing finding (important, differs from the anyOf sibling): reverted the guard, then tried the anyOf sibling's exact trick (a deliberately-broken sibling property alongside the oneOf, e.g. \"corrupt\": {\"type\": 1}) to see if the fix is externally observable. It is NOT — with the guard removed, the error is byte-identical: \"unsupported schema shape at OnlyFallback: empty union\". Root cause: classify() short-circuits on the oneOf key before ever looking at sibling type/properties (unlike classifyAnyOf's object branch, which only fires when members[typeKey]==object). And critically, classifyOneOf's .taggedUnion path calls taggedUnionModel directly with no other processing in between - unlike .objectTaggedUnion, whose objectTaggedUnionModel calls structModel (processing sibling members) before ever re-deriving the union. Since both the classification-site throw and taggedUnionModel's downstream throw use the identical context/detail pair, there is no schema construction that can make classifyOneOf's guard produce an observably different result. Restored the guard afterward; confirmed the removed-experiment test file has zero diff vs HEAD.\n-\n-    Given this, I did not add a new compound regression test - doing so would just be another no-op pinning test duplicating the existing oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail, which the acceptance criteria explicitly said to avoid. The existing test still pins the (unchanged) final error. The fix itself is documented in-code as a directness/proximate-cause improvement only, confirmed by mutation testing, not an externally-observable behavior change.\n-\n-    Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n-  timestamp: 2026-07-28T19:07:44.773652+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","transaction_id":null}
{"id":"01KYN2Z4M6THECAQ1FYSJHMJMZ","timestamp":"2026-07-28T19:24:32.262908Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -13,8 +13,8 @@\n\n     Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n   timestamp: 2026-07-28T19:07:44.773652+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\n","reverse_patch":"--- original\n+++ modified\n@@ -13,8 +13,8 @@\n\n     Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n   timestamp: 2026-07-28T19:07:44.773652+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\n","transaction_id":null}
{"id":"01KYN2ZHTMCJHXKQXJE0Q16QQH","timestamp":"2026-07-28T19:24:45.780746Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -44,4 +44,8 @@\n\n - [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\n - [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\n-- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n\\ No newline at end of file\n+- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n+\n+## Review Findings (2026-07-28 14:18)\n+\n+- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\n","reverse_patch":"--- original\n+++ modified\n@@ -44,8 +44,4 @@\n\n - [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\n - [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\n-- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n-\n-## Review Findings (2026-07-28 14:18)\n-\n-- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\n+- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN306E2MBDHVG5EDMRKNTMF","timestamp":"2026-07-28T19:25:06.882565Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -13,8 +13,8 @@\n\n     Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n   timestamp: 2026-07-28T19:07:44.773652+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\n","reverse_patch":"--- original\n+++ modified\n@@ -13,8 +13,8 @@\n\n     Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n   timestamp: 2026-07-28T19:07:44.773652+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\n","transaction_id":null}
{"id":"01KYN4D1FH3KN2FEQS8TN6BD5S","timestamp":"2026-07-28T19:49:36.369751Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -13,6 +13,27 @@\n\n     Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n   timestamp: 2026-07-28T19:07:44.773652+00:00\n+- actor: claude-code\n+  id: 01kyn4d1fhrq8xg2z6a1g0ydsw\n+  text: |-\n+    Worked the one unchecked review-findings item: extracted the verbatim-duplicated `guard !modeled.isEmpty else { throw GeneratorError.unsupportedShape(context: name, detail: emptyUnionDetail) }` block shared by classifyOneOf and classifyAnyOf.\n+\n+    Decision on naming/reuse (per the task's explicit either/or): rather than adding a new differently-named helper (e.g. validateModeledVariantsNotEmpty) with a byte-identical body, I reused the existing validateNonEmptyUnion(_:name:) helper at the second call site too, since the check (empty-array guard, same thrown error) is literally identical regardless of which array (raw variants vs. the modeled/payload-bearing subset) is passed in. Both classifyOneOf and classifyAnyOf now call `try validateNonEmptyUnion(modeled, name: name)` instead of the duplicated inline guard. Expanded validateNonEmptyUnion's docstring to document both use cases (raw-variants emptiness at classification entry; modeled-subset emptiness after fallback filtering).\n+\n+    Ran two rounds of the double-check adversarial agent (bounded per really-done):\n+    - Round 1: found a factual overclaim in my first docstring draft (\"all four call sites previously duplicated this inline\" - false, only 2 of 4 did; the other 2 already called the helper). Fixed the wording.\n+    - Round 1 also flagged a comment (\"Symmetric with the `.taggedUnion` branch's own `!modeled.isEmpty` condition below\") as a stale reference to classifyOneOf. I verified this was a misreading - it self-references classifyAnyOf's own later arity-check branch (line ~665, untouched, still a literal `!modeled.isEmpty`), not classifyOneOf. Reworded anyway for unambiguous clarity.\n+    - Round 2: found two more pre-existing doc comments (emptyUnionDetail's docstring, and classifyOneOf's own docstring) still calling the now-converted-to-a-function-call check a \"guard\" - inconsistent with the \"guard\"->\"check\" wording I'd already applied elsewhere in the same diff. Fixed both for consistency.\n+\n+    Final verification (all fresh, after the last doc fix):\n+    - `swift build --build-tests`: clean, 0 warnings.\n+    - `swift test`: 198 tests/16 suites + 95 tests/12 suites = 293 tests / 28 suites / 0 failures / 0 warnings - matches the stated baseline exactly.\n+    - `swift package --allow-writing-to-package-directory generate-acp`: \"nothing regenerated\" - byte-identical codegen gate confirmed.\n+    - `mcp__sah__diagnostics check working`: 0 errors, 0 warnings.\n+    - Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (docstrings + the two guard->helper-call replacements); no swift format run; no markdown tables in this comment.\n+\n+    Leaving in doing for review.\n+  timestamp: 2026-07-28T19:49:36.369299+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","reverse_patch":"--- original\n+++ modified\n@@ -13,27 +13,6 @@\n\n     Verification: swift build --build-tests clean; swift test -> 293 tests / 28 suites / 0 failures / 0 warnings (198 in FoundationModelsACPTests + 95 in ACPGenerateTests), matching stated baseline exactly; swift package --allow-writing-to-package-directory generate-acp -> \"nothing regenerated\" (byte-identical). No swift format run. Diff is scoped to Sources/ACPGenerateCore/SchemaGenerator.swift only.\n   timestamp: 2026-07-28T19:07:44.773652+00:00\n-- actor: claude-code\n-  id: 01kyn4d1fhrq8xg2z6a1g0ydsw\n-  text: |-\n-    Worked the one unchecked review-findings item: extracted the verbatim-duplicated `guard !modeled.isEmpty else { throw GeneratorError.unsupportedShape(context: name, detail: emptyUnionDetail) }` block shared by classifyOneOf and classifyAnyOf.\n-\n-    Decision on naming/reuse (per the task's explicit either/or): rather than adding a new differently-named helper (e.g. validateModeledVariantsNotEmpty) with a byte-identical body, I reused the existing validateNonEmptyUnion(_:name:) helper at the second call site too, since the check (empty-array guard, same thrown error) is literally identical regardless of which array (raw variants vs. the modeled/payload-bearing subset) is passed in. Both classifyOneOf and classifyAnyOf now call `try validateNonEmptyUnion(modeled, name: name)` instead of the duplicated inline guard. Expanded validateNonEmptyUnion's docstring to document both use cases (raw-variants emptiness at classification entry; modeled-subset emptiness after fallback filtering).\n-\n-    Ran two rounds of the double-check adversarial agent (bounded per really-done):\n-    - Round 1: found a factual overclaim in my first docstring draft (\"all four call sites previously duplicated this inline\" - false, only 2 of 4 did; the other 2 already called the helper). Fixed the wording.\n-    - Round 1 also flagged a comment (\"Symmetric with the `.taggedUnion` branch's own `!modeled.isEmpty` condition below\") as a stale reference to classifyOneOf. I verified this was a misreading - it self-references classifyAnyOf's own later arity-check branch (line ~665, untouched, still a literal `!modeled.isEmpty`), not classifyOneOf. Reworded anyway for unambiguous clarity.\n-    - Round 2: found two more pre-existing doc comments (emptyUnionDetail's docstring, and classifyOneOf's own docstring) still calling the now-converted-to-a-function-call check a \"guard\" - inconsistent with the \"guard\"->\"check\" wording I'd already applied elsewhere in the same diff. Fixed both for consistency.\n-\n-    Final verification (all fresh, after the last doc fix):\n-    - `swift build --build-tests`: clean, 0 warnings.\n-    - `swift test`: 198 tests/16 suites + 95 tests/12 suites = 293 tests / 28 suites / 0 failures / 0 warnings - matches the stated baseline exactly.\n-    - `swift package --allow-writing-to-package-directory generate-acp`: \"nothing regenerated\" - byte-identical codegen gate confirmed.\n-    - `mcp__sah__diagnostics check working`: 0 errors, 0 warnings.\n-    - Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (docstrings + the two guard->helper-call replacements); no swift format run; no markdown tables in this comment.\n-\n-    Leaving in doing for review.\n-  timestamp: 2026-07-28T19:49:36.369299+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","transaction_id":null}
{"id":"01KYN4DK63F2M7GGXG7F6HGNF1","timestamp":"2026-07-28T19:49:54.499811Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -38,35 +38,4 @@\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\n-\n-Surfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\n-\n-## The gap\n-\n-`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\n-\n-```swift\n-guard fallbacksAreGenuineCatchAlls(of: variants) else {\n-    return .deferredUnion(keyword: Self.oneOfKey)\n-}\n-return .taggedUnion\n-```\n-\n-It unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \"reached less directly\" pattern the sibling task just closed for `anyOf`'s object branch.\n-\n-This is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\n-\n-## Suggested fix\n-\n-Mirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\n-\n-## Acceptance criteria\n-\n-- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\n-- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\n-- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n-\n-## Review Findings (2026-07-28 14:18)\n-\n-- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -38,4 +38,35 @@\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\n\\ No newline at end of file\n+## Why this card exists\n+\n+Surfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\n+\n+## The gap\n+\n+`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\n+\n+```swift\n+guard fallbacksAreGenuineCatchAlls(of: variants) else {\n+    return .deferredUnion(keyword: Self.oneOfKey)\n+}\n+return .taggedUnion\n+```\n+\n+It unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \"reached less directly\" pattern the sibling task just closed for `anyOf`'s object branch.\n+\n+This is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\n+\n+## Suggested fix\n+\n+Mirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\n+\n+## Acceptance criteria\n+\n+- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\n+- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\n+- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\n+\n+## Review Findings (2026-07-28 14:18)\n+\n+- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\n","transaction_id":null}
{"id":"01KYN53VPNNCWNTQQBHYGB23WK","timestamp":"2026-07-28T20:02:04.117761Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -34,8 +34,8 @@\n\n     Leaving in doing for review.\n   timestamp: 2026-07-28T19:49:36.369299+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -34,8 +34,8 @@\n\n     Leaving in doing for review.\n   timestamp: 2026-07-28T19:49:36.369299+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN54ENQD8V6XMA66QK64DTV","timestamp":"2026-07-28T20:02:23.543071Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -38,4 +38,4 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\"methods\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\"methods\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\"    public static let \\\\(Self.methodsPropertyName): [MethodInfo] = [\\\"` and `\\\"        public static let \\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -38,4 +38,4 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\"methods\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\"methods\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\"    public static let \\\\(Self.methodsPropertyName): [MethodInfo] = [\\\"` and `\\\"        public static let \\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN55DPTQ7715SHJ6RFD2215","timestamp":"2026-07-28T20:02:55.322175Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -34,8 +34,8 @@\n\n     Leaving in doing for review.\n   timestamp: 2026-07-28T19:49:36.369299+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\"methods\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\"methods\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\"    public static let \\\\(Self.methodsPropertyName): [MethodInfo] = [\\\"` and `\\\"        public static let \\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -34,8 +34,8 @@\n\n     Leaving in doing for review.\n   timestamp: 2026-07-28T19:49:36.369299+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\"methods\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\"methods\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\"    public static let \\\\(Self.methodsPropertyName): [MethodInfo] = [\\\"` and `\\\"        public static let \\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN5D7WXQMWR3RQ0SRQMXY4R","timestamp":"2026-07-28T20:07:11.517640Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -34,6 +34,24 @@\n\n     Leaving in doing for review.\n   timestamp: 2026-07-28T19:49:36.369299+00:00\n+- actor: claude-code\n+  id: 01kyn5d7wxx1hfhtd77nsgrtzd\n+  text: |-\n+    Worked the second review-findings item (2026-07-28 14:54): the hardcoded \"methods\" property-name literal duplicated in the two routing-table header declarations inside extension Emitter.\n+\n+    Stale line numbers verified and corrected: the cited 2317/2357 (and later 2497/2537) no longer match; the actual current locations after the prior review-findings commit are line 2504 (methodTableDeclaration's header array, building ACPMethodTable) and line 2544 (unstableNamespaceDeclaration's header array, building Unstable.MethodTable). Found via grep -n \"methods\" across the whole file, confirmed the only two \"public static let methods: [...] = [\" header declarations.\n+\n+    Fix: added private static let methodsPropertyName = \"methods\" (with a docstring on the lockstep rationale) right after the existing sideCase helper near the top of extension Emitter, then changed both header lines to interpolate \\(Self.methodsPropertyName) instead of the literal word, e.g. \"    public static let \\(Self.methodsPropertyName): [MethodInfo] = [\". Follows the file's existing private static let xKey = \"...\" / Self.xKey convention used throughout (oneOfKey, typeKey, emptyUnionDetail, etc.) rather than inventing a new pattern.\n+\n+    Verification (all fresh):\n+    - swift build --build-tests: clean, 0 warnings.\n+    - swift test: 198 tests/16 suites + 95 tests/12 suites = 293 tests/28 suites/0 failures/0 warnings - matches baseline exactly.\n+    - swift package --allow-writing-to-package-directory generate-acp: \"nothing regenerated\" - byte-identical, including MethodTable.generated.swift specifically (the routing-table generator this touches). git status confirms no generated files changed.\n+    - mcp__sah__diagnostics check working: 0 errors, 0 warnings.\n+    - Adversarial double-check agent: PASS, no findings, independently re-ran build/test/codegen fresh.\n+\n+    Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n+  timestamp: 2026-07-28T20:07:11.517346+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","reverse_patch":"--- original\n+++ modified\n@@ -34,24 +34,6 @@\n\n     Leaving in doing for review.\n   timestamp: 2026-07-28T19:49:36.369299+00:00\n-- actor: claude-code\n-  id: 01kyn5d7wxx1hfhtd77nsgrtzd\n-  text: |-\n-    Worked the second review-findings item (2026-07-28 14:54): the hardcoded \"methods\" property-name literal duplicated in the two routing-table header declarations inside extension Emitter.\n-\n-    Stale line numbers verified and corrected: the cited 2317/2357 (and later 2497/2537) no longer match; the actual current locations after the prior review-findings commit are line 2504 (methodTableDeclaration's header array, building ACPMethodTable) and line 2544 (unstableNamespaceDeclaration's header array, building Unstable.MethodTable). Found via grep -n \"methods\" across the whole file, confirmed the only two \"public static let methods: [...] = [\" header declarations.\n-\n-    Fix: added private static let methodsPropertyName = \"methods\" (with a docstring on the lockstep rationale) right after the existing sideCase helper near the top of extension Emitter, then changed both header lines to interpolate \\(Self.methodsPropertyName) instead of the literal word, e.g. \"    public static let \\(Self.methodsPropertyName): [MethodInfo] = [\". Follows the file's existing private static let xKey = \"...\" / Self.xKey convention used throughout (oneOfKey, typeKey, emptyUnionDetail, etc.) rather than inventing a new pattern.\n-\n-    Verification (all fresh):\n-    - swift build --build-tests: clean, 0 warnings.\n-    - swift test: 198 tests/16 suites + 95 tests/12 suites = 293 tests/28 suites/0 failures/0 warnings - matches baseline exactly.\n-    - swift package --allow-writing-to-package-directory generate-acp: \"nothing regenerated\" - byte-identical, including MethodTable.generated.swift specifically (the routing-table generator this touches). git status confirms no generated files changed.\n-    - mcp__sah__diagnostics check working: 0 errors, 0 warnings.\n-    - Adversarial double-check agent: PASS, no findings, independently re-ran build/test/codegen fresh.\n-\n-    Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n-  timestamp: 2026-07-28T20:07:11.517346+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","transaction_id":null}
{"id":"01KYN5DXK0N4KT3CKP88MQ525K","timestamp":"2026-07-28T20:07:33.728362Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -56,4 +56,4 @@\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\"methods\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\"methods\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\"    public static let \\\\(Self.methodsPropertyName): [MethodInfo] = [\\\"` and `\\\"        public static let \\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -56,4 +56,4 @@\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\"reached less directly\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\"nothing regenerated\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\"methods\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\"methods\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\"    public static let \\\\(Self.methodsPropertyName): [MethodInfo] = [\\\"` and `\\\"        public static let \\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN61GB97ZKPXEB2VZJ1TC29","timestamp":"2026-07-28T20:18:15.529519Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -52,8 +52,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n   timestamp: 2026-07-28T20:07:11.517346+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -52,8 +52,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n   timestamp: 2026-07-28T20:07:11.517346+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN6G04YS5ZCG93KD9NQA81Y","timestamp":"2026-07-28T20:26:10.462102Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -56,4 +56,8 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\"reached less directly\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\"nothing regenerated\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\"methods\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\"methods\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\"    public static let \\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\"` and `\\\\\"        public static let \\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\"methods\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\"methods\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\"...\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\"nothing regenerated\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n+\n+## Review Findings (2026-07-28 15:12)\n+\n+- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -56,8 +56,4 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\"reached less directly\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\"nothing regenerated\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\"methods\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\"methods\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\"    public static let \\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\"` and `\\\\\"        public static let \\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\"methods\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\"methods\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\"...\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\"nothing regenerated\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n-\n-## Review Findings (2026-07-28 15:12)\n-\n-- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN6HAG18GYHDYWNCKPAYSQN","timestamp":"2026-07-28T20:26:53.825830Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -56,7 +56,7 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\"reached less directly\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\"nothing regenerated\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\"methods\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\"methods\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\"    public static let \\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\"` and `\\\\\"        public static let \\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\"methods\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\"methods\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\"...\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\"nothing regenerated\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\n ## Review Findings (2026-07-28 15:12)\n\n","reverse_patch":"--- original\n+++ modified\n@@ -56,7 +56,7 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\"reached less directly\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\"nothing regenerated\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\"methods\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\"methods\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\"    public static let \\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\"` and `\\\\\"        public static let \\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\"methods\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\"methods\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\"...\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\"nothing regenerated\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n\n ## Review Findings (2026-07-28 15:12)\n\n","transaction_id":null}
{"id":"01KYN6JV6FNQ4538B8Z9MA4FA3","timestamp":"2026-07-28T20:27:43.695053Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -52,8 +52,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n   timestamp: 2026-07-28T20:07:11.517346+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n","reverse_patch":"--- original\n+++ modified\n@@ -52,8 +52,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n   timestamp: 2026-07-28T20:07:11.517346+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n","transaction_id":null}
{"id":"01KYN6RVTK61GH0ZYRXMEC6J5S","timestamp":"2026-07-28T20:31:00.947518Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -60,4 +60,8 @@\n\n ## Review Findings (2026-07-28 15:12)\n\n-- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n\\ No newline at end of file\n+- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n+\n+  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \"this function\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \"below\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\n+\n+  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \"nothing regenerated\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n","reverse_patch":"--- original\n+++ modified\n@@ -60,8 +60,4 @@\n\n ## Review Findings (2026-07-28 15:12)\n\n-- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n-\n-  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \"this function\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \"below\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\n-\n-  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \"nothing regenerated\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n+- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN6SDKZ5CNV0ZFA2S6SED36","timestamp":"2026-07-28T20:31:19.167486Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -52,6 +52,28 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n   timestamp: 2026-07-28T20:07:11.517346+00:00\n+- actor: claude-code\n+  id: 01kyn6sdkzahwghef24zbmvd3c\n+  text: |-\n+    Worked the third review-findings item (2026-07-28 15:12): the long conditional chain in the object-typed/discriminated-union classification logic.\n+\n+    Stale line numbers verified: 460/462/467/471/480/483/485 no longer matched, but the cited 3-AND-condition line (modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)) is unique to classifyAnyOf (currently spanning roughly lines 622-641 after the edit) - classifyOneOf has no such line. Confirmed classifyAnyOf has exactly one call site (line 283) via grep, so this is a low-blast-radius private-function refactor.\n+\n+    Extracted two private helpers immediately after classifyAnyOf:\n+    - classifyObjectTypedAnyOf(name:modeled:) - the object-typed branch: validateNonEmptyUnion(modeled,...), the allOf-flattening allSatisfy/contains checks, and the .objectTaggedUnion/.objectValueUnion/.deferredUnion split.\n+    - detectAnyOfDiscriminatedUnion(variants:modeled:) - the arity/discriminator detection: the 3-AND-condition line and the trailing hasConstDiscriminator check for .discriminatedUnion vs .deferredUnion.\n+\n+    classifyAnyOf itself is now five short, linear steps: validate non-empty union, scalar-enum check, catch-all-fallback guard, derive modeled, then branch to one helper or the other based on whether members[typeKey]==object. Moved each branch's explanatory comments into the corresponding new helper's docstring instead of leaving \"this function\"-relative inline comments that would go stale once the code moved; updated the one cross-reference comment (in the object-typed branch, previously \"Symmetric with this function's own arity condition below\") to name detectAnyOfDiscriminatedUnion explicitly rather than \"below\". Did not touch classifyOneOf - the task and the cited line both point at classifyAnyOf only.\n+\n+    Verification (all fresh):\n+    - swift build --build-tests: clean, 0 warnings.\n+    - swift test: 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings - matches the stated baseline exactly.\n+    - swift package --allow-writing-to-package-directory generate-acp: \"nothing regenerated\" (schema hash unchanged) - byte-identical; git status --porcelain confirms no generated files touched.\n+    - mcp__sah__diagnostics check working: 0 errors, 0 warnings.\n+    - No swift format run.\n+\n+    Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n+  timestamp: 2026-07-28T20:31:19.167181+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","reverse_patch":"--- original\n+++ modified\n@@ -52,28 +52,6 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift. No swift format run. Leaving in doing for review.\n   timestamp: 2026-07-28T20:07:11.517346+00:00\n-- actor: claude-code\n-  id: 01kyn6sdkzahwghef24zbmvd3c\n-  text: |-\n-    Worked the third review-findings item (2026-07-28 15:12): the long conditional chain in the object-typed/discriminated-union classification logic.\n-\n-    Stale line numbers verified: 460/462/467/471/480/483/485 no longer matched, but the cited 3-AND-condition line (modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)) is unique to classifyAnyOf (currently spanning roughly lines 622-641 after the edit) - classifyOneOf has no such line. Confirmed classifyAnyOf has exactly one call site (line 283) via grep, so this is a low-blast-radius private-function refactor.\n-\n-    Extracted two private helpers immediately after classifyAnyOf:\n-    - classifyObjectTypedAnyOf(name:modeled:) - the object-typed branch: validateNonEmptyUnion(modeled,...), the allOf-flattening allSatisfy/contains checks, and the .objectTaggedUnion/.objectValueUnion/.deferredUnion split.\n-    - detectAnyOfDiscriminatedUnion(variants:modeled:) - the arity/discriminator detection: the 3-AND-condition line and the trailing hasConstDiscriminator check for .discriminatedUnion vs .deferredUnion.\n-\n-    classifyAnyOf itself is now five short, linear steps: validate non-empty union, scalar-enum check, catch-all-fallback guard, derive modeled, then branch to one helper or the other based on whether members[typeKey]==object. Moved each branch's explanatory comments into the corresponding new helper's docstring instead of leaving \"this function\"-relative inline comments that would go stale once the code moved; updated the one cross-reference comment (in the object-typed branch, previously \"Symmetric with this function's own arity condition below\") to name detectAnyOfDiscriminatedUnion explicitly rather than \"below\". Did not touch classifyOneOf - the task and the cited line both point at classifyAnyOf only.\n-\n-    Verification (all fresh):\n-    - swift build --build-tests: clean, 0 warnings.\n-    - swift test: 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings - matches the stated baseline exactly.\n-    - swift package --allow-writing-to-package-directory generate-acp: \"nothing regenerated\" (schema hash unchanged) - byte-identical; git status --porcelain confirms no generated files touched.\n-    - mcp__sah__diagnostics check working: 0 errors, 0 warnings.\n-    - No swift format run.\n-\n-    Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n-  timestamp: 2026-07-28T20:31:19.167181+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","transaction_id":null}
{"id":"01KYN7GJXYTDG4BFJSVC09VJXV","timestamp":"2026-07-28T20:43:58.270997Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -74,8 +74,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n   timestamp: 2026-07-28T20:31:19.167181+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n","reverse_patch":"--- original\n+++ modified\n@@ -74,8 +74,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n   timestamp: 2026-07-28T20:31:19.167181+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n","transaction_id":null}
{"id":"01KYN7HKMW5JGDSG851SJ7TRZB","timestamp":"2026-07-28T20:44:31.772710Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -78,12 +78,4 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n-\n-## Review Findings (2026-07-28 15:12)\n-\n-- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n-\n-  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \"this function\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \"below\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\n-\n-  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \"nothing regenerated\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \\\"this function\\\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \\\"below\\\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \\\"nothing regenerated\\\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\\\"definition \\\\(name) is neither a \\\" + Self.definitionSuffixes.map { \\\"\\\\($0)\\\" }.joined(separator: \\\", a \\\") + \\\" suffix\\\"`.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -78,4 +78,12 @@\n position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \\\"this function\\\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \\\"below\\\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \\\"nothing regenerated\\\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\\\"definition \\\\(name) is neither a \\\" + Self.definitionSuffixes.map { \\\"\\\\($0)\\\" }.joined(separator: \\\", a \\\") + \\\" suffix\\\"`.\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\n+\n+## Review Findings (2026-07-28 15:12)\n+\n+- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\n+\n+  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \"this function\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \"below\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\n+\n+  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \"nothing regenerated\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n","transaction_id":null}
{"id":"01KYN7J9D8XYR57QHND0YKCP0T","timestamp":"2026-07-28T20:44:54.056242Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -74,8 +74,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n   timestamp: 2026-07-28T20:31:19.167181+00:00\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \\\"this function\\\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \\\"below\\\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \\\"nothing regenerated\\\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\\\"definition \\\\(name) is neither a \\\" + Self.definitionSuffixes.map { \\\"\\\\($0)\\\" }.joined(separator: \\\", a \\\") + \\\" suffix\\\"`.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -74,8 +74,8 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n   timestamp: 2026-07-28T20:31:19.167181+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n ## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \\\"this function\\\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \\\"below\\\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \\\"nothing regenerated\\\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\\\"definition \\\\(name) is neither a \\\" + Self.definitionSuffixes.map { \\\"\\\\($0)\\\" }.joined(separator: \\\", a \\\") + \\\" suffix\\\"`.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN7ZR065QN2BR81SK9RVK2Z","timestamp":"2026-07-28T20:52:14.982248Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -78,4 +78,4 @@\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \\\"this function\\\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \\\"below\\\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \\\"nothing regenerated\\\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\\\"definition \\\\(name) is neither a \\\" + Self.definitionSuffixes.map { \\\"\\\\($0)\\\" }.joined(separator: \\\", a \\\") + \\\" suffix\\\"`.\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\"reached less directly\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\"nothing regenerated\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\"methods\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\"methods\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\"` and `\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\"methods\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\"methods\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\"...\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\"nothing regenerated\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \"this function\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \"below\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \"nothing regenerated\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\"definition \\(name) is neither a \" + Self.definitionSuffixes.map { \"\\($0)\" }.joined(separator: \", a \") + \" suffix\"`.\\n\\n  Resolved: the cited line number (2691) was stale; the actual message lives in `methodModel(routingKey:wireMethod:side:definitionNames:)`'s suffix-matching guard, current wording exactly `\"definition \\(name) is neither a \\(Self.requestSuffix), a \\(Self.responseSuffix), nor a \\(Self.notificationSuffix)\"`. The suggested fix's literal replacement text (ending in `+ \" suffix\"`) did not match the actual grammar (the real message has no trailing word \"suffix\" at all), so it was adapted rather than applied verbatim, per the task's own caveat.\\n\\n  Added a new `private static var definitionSuffixesDescription: String` computed property immediately below `definitionSuffixes`, which maps each suffix to `\"a \\($0)\"` and joins them with an Oxford-style `\", \"`/`\", nor \"` separator (`\"a Notification, a Response, nor a Request\"`), then changed the guard's `detail:` to `\"definition \\(name) is neither \\(Self.definitionSuffixesDescription)\"`. This derives the full listing from `definitionSuffixes` itself (currently `[notificationSuffix, responseSuffix, requestSuffix]`, in match-priority order, not display order) so a future fourth suffix added to that array automatically appears in the error text with no further edits. Confirmed no test pins the old wording/order (`grep` across `Tests/` for `is neither a`, `definitionSuffixes`, `requestSuffix`/`responseSuffix`/`notificationSuffix` found only unrelated matches) - only `swift test`'s green baseline count, not any exact-string assertion, needed to hold. Left the unrelated, differently-shaped message at the `\"form neither a \\(Self.requestSuffix)/\\(Self.responseSuffix) pair nor a single \\(Self.notificationSuffix)\"` site (the Request/Response-pair-vs-single-Notification arity check, a few lines below) untouched - it is not the message this finding cited and does not have the same stale-list shape (it describes a structural pairing constraint, not an enumerable suffix list).\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` -> 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` -> \"nothing regenerated\" (schema hash unchanged), byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` -> 0 errors, 0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -78,4 +78,4 @@\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n ---\n-## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\"reached less directly\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\"nothing regenerated\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\"methods\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\"methods\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\"` and `\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\"methods\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\"methods\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\"...\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\"nothing regenerated\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \"this function\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \"below\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \"nothing regenerated\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\"definition \\(name) is neither a \" + Self.definitionSuffixes.map { \"\\($0)\" }.joined(separator: \", a \") + \" suffix\"`.\\n\\n  Resolved: the cited line number (2691) was stale; the actual message lives in `methodModel(routingKey:wireMethod:side:definitionNames:)`'s suffix-matching guard, current wording exactly `\"definition \\(name) is neither a \\(Self.requestSuffix), a \\(Self.responseSuffix), nor a \\(Self.notificationSuffix)\"`. The suggested fix's literal replacement text (ending in `+ \" suffix\"`) did not match the actual grammar (the real message has no trailing word \"suffix\" at all), so it was adapted rather than applied verbatim, per the task's own caveat.\\n\\n  Added a new `private static var definitionSuffixesDescription: String` computed property immediately below `definitionSuffixes`, which maps each suffix to `\"a \\($0)\"` and joins them with an Oxford-style `\", \"`/`\", nor \"` separator (`\"a Notification, a Response, nor a Request\"`), then changed the guard's `detail:` to `\"definition \\(name) is neither \\(Self.definitionSuffixesDescription)\"`. This derives the full listing from `definitionSuffixes` itself (currently `[notificationSuffix, responseSuffix, requestSuffix]`, in match-priority order, not display order) so a future fourth suffix added to that array automatically appears in the error text with no further edits. Confirmed no test pins the old wording/order (`grep` across `Tests/` for `is neither a`, `definitionSuffixes`, `requestSuffix`/`responseSuffix`/`notificationSuffix` found only unrelated matches) - only `swift test`'s green baseline count, not any exact-string assertion, needed to hold. Left the unrelated, differently-shaped message at the `\"form neither a \\(Self.requestSuffix)/\\(Self.responseSuffix) pair nor a single \\(Self.notificationSuffix)\"` site (the Request/Response-pair-vs-single-Notification arity check, a few lines below) untouched - it is not the message this finding cited and does not have the same stale-list shape (it describes a structural pairing constraint, not an enumerable suffix list).\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` -> 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` -> \"nothing regenerated\" (schema hash unchanged), byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` -> 0 errors, 0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\n\\ No newline at end of file\n+## Why this card exists\\n\\nSurfaced by the adversarial double-check on task 01KYKCX3XZWEP3PZ77826RC4A5 (^26rc4a5), which added a `!modeled.isEmpty` guard to `classifyAnyOf`'s object branch so an object-typed `anyOf` whose variants are all `not`-guarded catch-alls throws directly at the classification site instead of relying on a downstream re-derivation to catch it.\\n\\n## The gap\\n\\n`classifyOneOf` (in `Sources/ACPGenerateCore/SchemaGenerator.swift`) has the same shape, once `fallbacksAreGenuineCatchAlls` passes:\\n\\n```swift\\nguard fallbacksAreGenuineCatchAlls(of: variants) else {\\n    return .deferredUnion(keyword: Self.oneOfKey)\\n}\\nreturn .taggedUnion\\n```\\n\\nIt unconditionally returns `.taggedUnion` without ever checking whether a `modeled`-equivalent (payload-bearing) subset is non-empty. If every variant of a `oneOf` were a genuine `not`-guarded catch-all, classification would still return `.taggedUnion`, and only `taggedUnionModel`'s own `guard let discriminator else { throw ... }` would catch it downstream — the exact \\\\\\\"reached less directly\\\\\\\" pattern the sibling task just closed for `anyOf`'s object branch.\\n\\nThis is already covered today by an *existing* test — `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` — which pins the downstream throw. So this is not urgent and not silently broken; it is the same directness/proximate-cause improvement, not yet applied to `classifyOneOf`.\\n\\n## Suggested fix\\n\\nMirror `classifyAnyOf`'s object-branch fix: compute the modeled (non-`not`-guarded) variant set in `classifyOneOf` and guard on `!modeled.isEmpty` before returning `.taggedUnion`, throwing `GeneratorError.unsupportedShape(context: name, detail: Self.emptyUnionDetail)` directly when empty.\\n\\n## Acceptance criteria\\n\\n- [ ] `classifyOneOf` throws `GeneratorError.unsupportedShape(detail: emptyUnionDetail)` directly when every `oneOf` variant is a genuine catch-all, rather than relying on `taggedUnionModel`'s downstream re-derivation.\\n- [ ] Existing test `TaggedUnionTests.oneOfWhoseOnlyVariantIsTheUnknownFallbackFailsOnTheEmptyUnionDetail` still passes (same final error, now thrown more directly) — consider adding a compound-schema regression test analogous to `AnyOfUnionTests.objectAnyOfWhoseOnlyVariantIsTheUnknownFallbackFailsAtClassification`, which uses a deliberately-broken sibling property to make the fix's directness observable/load-bearing rather than a no-op pinning test.\\n- [ ] `swift test` green, `swift package generate-acp` byte-identical regeneration.\\n\\n## Review Findings (2026-07-28 14:18)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:383` — The guard checking `!modeled.isEmpty` and throwing `emptyUnionDetail` error is verbatim duplicated in both `classifyOneOf` (line 383–385) and `classifyAnyOf` (line 525–527), creating maintenance burden: fixes to the condition or error message must be applied in two places. Extract a shared helper function `validateModeledVariantsNotEmpty(_ modeled: [JSONValue], name: String) throws` and call it in both `classifyOneOf` and `classifyAnyOf` immediately after deriving the `modeled` variable. This mirrors the existing `validateNonEmptyUnion` extraction pattern.\\n\\n  Resolved by reusing the existing `validateNonEmptyUnion(_:name:)` helper at both `modeled` call sites (rather than adding a new, differently-named, byte-identical helper) — both `classifyOneOf` and `classifyAnyOf` now call `try validateNonEmptyUnion(modeled, name: name)`. Docstrings updated accordingly. Verified: `swift build --build-tests` clean; `swift test` 293 tests/28 suites/0 failures/0 warnings; `swift package generate-acp` byte-identical (\\\\\\\"nothing regenerated\\\\\\\"). See task comments for the two adversarial double-check rounds and the doc-wording fixes each produced.\\n\\n## Review Findings (2026-07-28 14:54)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:2357` — The string `\\\\\\\"methods\\\\\\\"` (as a property name) is hardcoded in both the stable and unstable routing table declarations. It appears at line 2317 (in stable `ACPMethodTable`) and line 2357 (in unstable `MethodTable`). If the property name must change in the future, both hardcoded locations must be updated in lockstep. Extract `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` as a constant, then use string interpolation in both header arrays to reference it: `\\\\\\\"    public static let \\\\\\\\(Self.methodsPropertyName): [MethodInfo] = [\\\\\\\"` and `\\\\\\\"        public static let \\\\\\\\(Self.methodsPropertyName): [UnstableMethodInfo] = [\\\\\\\"` (requires converting static header arrays to computed properties or string interpolation).\\n\\n  Resolved: the cited line numbers (2317/2357) were stale by the time this was worked; the actual current locations were line 2504 (`methodTableDeclaration`'s header, `ACPMethodTable`) and line 2544 (`unstableNamespaceDeclaration`'s header, `Unstable.MethodTable`), confirmed via `grep -n \\\\\\\"methods\\\\\\\"` across the whole file. Added `private static let methodsPropertyName = \\\\\\\"methods\\\\\\\"` (with docstring) right after the existing `sideCase` helper in `extension Emitter`, and both header lines now interpolate `\\\\\\\\(Self.methodsPropertyName)` instead of the literal, matching the file's existing `private static let xKey = \\\\\\\"...\\\\\\\"` / `Self.xKey` convention. Verified: `swift build --build-tests` clean, 0 warnings; `swift test` 293 tests/28 suites/0 failures/0 warnings (unchanged baseline); `swift package generate-acp` \\\\\\\"nothing regenerated\\\\\\\" — byte-identical, including `MethodTable.generated.swift` specifically; `mcp__sah__diagnostics check working` 0 errors/0 warnings; adversarial double-check agent PASS, no findings. See task comments for full detail.\\n\\n## Review Findings (2026-07-28 15:12)\\n\\n- [x] `Sources/ACPGenerateCore/SchemaGenerator.swift:460` — Long conditional chain with complex boolean logic: 5+ sequential if/guard statements (lines 462, 467, 471, 480, 483) with line 480 combining 3 AND conditions (`modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)`), making control flow hard to follow and reason about. Extract the object-typed branch (lines 471–479) and the discriminated-union detection logic (lines 480–485) into separate helper functions to clarify intent and reduce decision density in the main function.\\n\\n  Resolved: the cited line numbers (460/462/467/471/480/483/485) were stale; the actual function was `classifyAnyOf` (confirmed by the exact `modeled.count < variants.count, !modeled.isEmpty, modeled.allSatisfy(hasConstDiscriminator)` line cited, which only exists in `classifyAnyOf`, not `classifyOneOf`). Extracted two private helpers immediately after `classifyAnyOf`: `classifyObjectTypedAnyOf(name:modeled:)` (the object-typed branch — `validateNonEmptyUnion`, the `allOf`-flattening check, and the `.objectTaggedUnion`/`.objectValueUnion`/`.deferredUnion` split) and `detectAnyOfDiscriminatedUnion(variants:modeled:)` (the arity/discriminator check — the 3-AND-condition line and the trailing `hasConstDiscriminator` check). `classifyAnyOf` itself now reads as five short steps: validate non-empty, scalar-enum check, catch-all-fallback guard, branch to one helper or the other. Moved each branch's explanatory comments into the new helper's docstring rather than leaving them as inline comments referring to \\\"this function\\\" (which would have gone stale/misleading once the code moved), and updated the one cross-reference comment in `classifyObjectTypedAnyOf` that pointed at the arity condition \\\"below\\\" to name `detectAnyOfDiscriminatedUnion` explicitly. Confirmed single call site for `classifyAnyOf` (line 283) — no other callers affected.\\n\\n  Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` → 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` → \\\"nothing regenerated\\\" (schema hash unchanged) — byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` → 0 errors/0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\\n\\n## Review Findings (2026-07-28 15:38)\\n\\n- [ ] `Sources/ACPGenerateCore/SchemaGenerator.swift:2691` — Error message hardcodes which suffixes are valid rather than deriving from the `definitionSuffixes` array. If a new suffix is added to the array, this error message becomes stale and incomplete. Construct the error message text by joining `definitionSuffixes` with appropriate separators: `\\\"definition \\\\(name) is neither a \\\" + Self.definitionSuffixes.map { \\\"\\\\($0)\\\" }.joined(separator: \\\", a \\\") + \\\" suffix\\\"`.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KYN87N65SD0122S48YEEV8WA","timestamp":"2026-07-28T20:56:34.245654Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -74,6 +74,23 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n   timestamp: 2026-07-28T20:31:19.167181+00:00\n+- actor: claude-code\n+  id: 01kyn87n65jct8nkv6t622f6dx\n+  text: |-\n+    Worked the fourth review-findings item (2026-07-28 15:38): the hardcoded suffix listing in the unrecognized-definition-suffix error message.\n+\n+    Verified current wording before touching anything: at the guard in methodModel(routingKey:wireMethod:side:definitionNames:), the message reads exactly \"definition \\(name) is neither a \\(Self.requestSuffix), a \\(Self.responseSuffix), nor a \\(Self.notificationSuffix)\" - no trailing word \"suffix\" as the task's suggested literal replacement assumed, so I adapted rather than pasted it verbatim (per the task's own caveat to check before applying literally).\n+\n+    Fix: added a private static var definitionSuffixesDescription: String computed property immediately below the existing definitionSuffixes array, mapping each entry to \"a \\($0)\" and Oxford-joining them (\", \" between all but the last, \", nor \" before the last), then changed the guard's detail: argument to interpolate that computed property instead of the three hardcoded Self.xSuffix references. definitionSuffixes is [notificationSuffix, responseSuffix, requestSuffix] (kept in its existing match-priority order, not display order - re-ordering it was out of scope and would only serve cosmetics), so the message now reads \"is neither a Notification, a Response, nor a Request\" - same grammatical shape as before, just reordered to match the single source of truth. Adding a fourth suffix to the array in the future now updates this message automatically.\n+\n+    Left the differently-shaped, unrelated message a few lines below (\"definitions [...] form neither a Request/Response pair nor a single Notification\") untouched - it describes a structural pairing constraint (pair vs. single), not an enumerable suffix listing, so it isn't the same kind of staleness risk and wasn't what this finding cited.\n+\n+    Confirmed via grep across Tests/ that nothing pins the old wording or suffix order (\"is neither a\", \"definitionSuffixes\", requestSuffix/responseSuffix/notificationSuffix all came back with only unrelated matches) - so no test needed updating.\n+\n+    Process note for the record: my first attempt at recording this in the kanban description via `update task` (hand-retyping the full ~9000-character prior description alongside my edit) silently corrupted an unrelated, untouched earlier paragraph - one escaped backslash-quote pair lost a backslash in the \"Why this card exists\" section, purely from manual transcription of a huge string. Caught it by diffing the stored file against a mechanically-generated expected version. Recovered by `git checkout --` on the task's .md (back to the last commit, 980fbec) and re-applying only the intended change via a small, anchored `files: edit file` old_string/new_string replacement (anchored on the unique tail string \"Leaving in `doing` for review.\"), which is exact and doesn't require retyping any preexisting content. Confirmed via `git diff` that the resulting change is a pure append of the new \"Review Findings (2026-07-28 15:38)\" section with nothing else touched.\n+\n+    Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` -> 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` -> \"nothing regenerated\" (schema hash unchanged), byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` -> 0 errors, 0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n+  timestamp: 2026-07-28T20:56:34.245187+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","reverse_patch":"--- original\n+++ modified\n@@ -74,23 +74,6 @@\n\n     Diff scoped entirely to Sources/ACPGenerateCore/SchemaGenerator.swift (the extraction plus doc-comment relocation/rewording). Checked the checkbox for this finding in the description using real newlines only in the section I touched; left the older, pre-existing \\n-corrupted narrative sections untouched per instructions. Leaving in doing for review.\n   timestamp: 2026-07-28T20:31:19.167181+00:00\n-- actor: claude-code\n-  id: 01kyn87n65jct8nkv6t622f6dx\n-  text: |-\n-    Worked the fourth review-findings item (2026-07-28 15:38): the hardcoded suffix listing in the unrecognized-definition-suffix error message.\n-\n-    Verified current wording before touching anything: at the guard in methodModel(routingKey:wireMethod:side:definitionNames:), the message reads exactly \"definition \\(name) is neither a \\(Self.requestSuffix), a \\(Self.responseSuffix), nor a \\(Self.notificationSuffix)\" - no trailing word \"suffix\" as the task's suggested literal replacement assumed, so I adapted rather than pasted it verbatim (per the task's own caveat to check before applying literally).\n-\n-    Fix: added a private static var definitionSuffixesDescription: String computed property immediately below the existing definitionSuffixes array, mapping each entry to \"a \\($0)\" and Oxford-joining them (\", \" between all but the last, \", nor \" before the last), then changed the guard's detail: argument to interpolate that computed property instead of the three hardcoded Self.xSuffix references. definitionSuffixes is [notificationSuffix, responseSuffix, requestSuffix] (kept in its existing match-priority order, not display order - re-ordering it was out of scope and would only serve cosmetics), so the message now reads \"is neither a Notification, a Response, nor a Request\" - same grammatical shape as before, just reordered to match the single source of truth. Adding a fourth suffix to the array in the future now updates this message automatically.\n-\n-    Left the differently-shaped, unrelated message a few lines below (\"definitions [...] form neither a Request/Response pair nor a single Notification\") untouched - it describes a structural pairing constraint (pair vs. single), not an enumerable suffix listing, so it isn't the same kind of staleness risk and wasn't what this finding cited.\n-\n-    Confirmed via grep across Tests/ that nothing pins the old wording or suffix order (\"is neither a\", \"definitionSuffixes\", requestSuffix/responseSuffix/notificationSuffix all came back with only unrelated matches) - so no test needed updating.\n-\n-    Process note for the record: my first attempt at recording this in the kanban description via `update task` (hand-retyping the full ~9000-character prior description alongside my edit) silently corrupted an unrelated, untouched earlier paragraph - one escaped backslash-quote pair lost a backslash in the \"Why this card exists\" section, purely from manual transcription of a huge string. Caught it by diffing the stored file against a mechanically-generated expected version. Recovered by `git checkout --` on the task's .md (back to the last commit, 980fbec) and re-applying only the intended change via a small, anchored `files: edit file` old_string/new_string replacement (anchored on the unique tail string \"Leaving in `doing` for review.\"), which is exact and doesn't require retyping any preexisting content. Confirmed via `git diff` that the resulting change is a pure append of the new \"Review Findings (2026-07-28 15:38)\" section with nothing else touched.\n-\n-    Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` -> 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` -> \"nothing regenerated\" (schema hash unchanged), byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` -> 0 errors, 0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n-  timestamp: 2026-07-28T20:56:34.245187+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","transaction_id":null}
{"id":"01KYN8CV4SR7CRNYWRMNC8HH7C","timestamp":"2026-07-28T20:59:24.185904Z","op":"update","item_id":"01KYMED438PSP949389RJRCGD7","forward_patch":"--- original\n+++ modified\n@@ -91,6 +91,15 @@\n\n     Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` -> 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` -> \"nothing regenerated\" (schema hash unchanged), byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` -> 0 errors, 0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n   timestamp: 2026-07-28T20:56:34.245187+00:00\n+- actor: claude-code\n+  id: 01kyn8cv4sa5hgwameeghj0v3f\n+  text: |-\n+    Adversarial double-check ran (per really-done): PASS overall, diff scoped correctly, tests/build/codegen all reconfirmed fresh, task description checkbox/resolution verified accurate, and confirmed the pre-existing description corruption in the \"Why this card exists\" section (the literal-backslash-n narrative blob) predates this task entirely (present at HEAD before any of my edits) and is unrelated to this change.\n+\n+    One advisory finding: definitionSuffixesDescription's two-item case would have produced \"a Y, nor a Z\" (comma before \"nor\"), which most style guides drop for exactly two items (\"a Y nor a Z\"). Currently unreachable (definitionSuffixes always has exactly 3 elements), but cheap and worth fixing for correctness/robustness rather than leaving a known-wrong branch. Reworked the computed property to special-case the two-item conjunction (\" nor \" without a comma) vs. three-or-more (\", nor \"). Re-verified fresh after this change: `swift build --build-tests` clean 0 warnings; `swift test` 198+95 = 293 tests/28 suites/0 failures/0 warnings; `swift package --allow-writing-to-package-directory generate-acp` \"nothing regenerated\"; `mcp__sah__diagnostics check working` 0 errors/0 warnings.\n+\n+    All four review-findings checkboxes in the description are now checked. Leaving in `doing` for review.\n+  timestamp: 2026-07-28T20:59:24.185475+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","reverse_patch":"--- original\n+++ modified\n@@ -91,15 +91,6 @@\n\n     Verification (all fresh): `swift build --build-tests` clean, 0 warnings; `swift test` -> 198 tests/16 suites (FoundationModelsACPTests) + 95 tests/12 suites (ACPGenerateTests) = 293 tests/28 suites/0 failures/0 warnings, matching the stated baseline exactly; `swift package --allow-writing-to-package-directory generate-acp` -> \"nothing regenerated\" (schema hash unchanged), byte-identical, confirmed also via `git status --porcelain` showing no generated files touched; `mcp__sah__diagnostics check working` -> 0 errors, 0 warnings. No `swift format` run. Diff scoped entirely to `Sources/ACPGenerateCore/SchemaGenerator.swift`. Leaving in `doing` for review.\n   timestamp: 2026-07-28T20:56:34.245187+00:00\n-- actor: claude-code\n-  id: 01kyn8cv4sa5hgwameeghj0v3f\n-  text: |-\n-    Adversarial double-check ran (per really-done): PASS overall, diff scoped correctly, tests/build/codegen all reconfirmed fresh, task description checkbox/resolution verified accurate, and confirmed the pre-existing description corruption in the \"Why this card exists\" section (the literal-backslash-n narrative blob) predates this task entirely (present at HEAD before any of my edits) and is unrelated to this change.\n-\n-    One advisory finding: definitionSuffixesDescription's two-item case would have produced \"a Y, nor a Z\" (comma before \"nor\"), which most style guides drop for exactly two items (\"a Y nor a Z\"). Currently unreachable (definitionSuffixes always has exactly 3 elements), but cheap and worth fixing for correctness/robustness rather than leaving a known-wrong branch. Reworked the computed property to special-case the two-item conjunction (\" nor \" without a comma) vs. three-or-more (\", nor \"). Re-verified fresh after this change: `swift build --build-tests` clean 0 warnings; `swift test` 198+95 = 293 tests/28 suites/0 failures/0 warnings; `swift package --allow-writing-to-package-directory generate-acp` \"nothing regenerated\"; `mcp__sah__diagnostics check working` 0 errors/0 warnings.\n-\n-    All four review-findings checkboxes in the description are now checked. Leaving in `doing` for review.\n-  timestamp: 2026-07-28T20:59:24.185475+00:00\n position_column: doing\n position_ordinal: '80'\n title: classifyOneOf has the same latent vacuous-classification gap classifyAnyOf's object-branch fix just closed\n","transaction_id":null}
