/// Namespace for the FoundationModelsACP package.
///
/// Reserved rather than occupied: the generated protocol types are emitted at
/// the top level, and nothing nests inside `ACP` yet. It exists so the later
/// hand-written layers — connections and transports — have one agreed anchor
/// to hang from instead of each picking its own.
public enum ACP {}
